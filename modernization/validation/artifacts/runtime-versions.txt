BEGIN runtime-version-report
report: system runtime and Python package versions measured in this
        checkout against modernization/requirements.txt and the
        environment contract of the technical specification
timestamp_utc: 2026-08-23T03:06:44Z
repository_root: . (every path of this report is relative to the repository
        root, so the report reads the same from every checkout)
host_release: Ubuntu 25.10
apt_suites: questing questing-updates questing-backports questing-security
status_label: validated against local substitute, not AWS
aws_branch: formal AWS validation remains OPEN; this report covers versions only
rationale: see modernization/docs/decision-log.md

system runtimes (required, minimum accepted, measured, verdict):
  python (venv interpreter)  required=3.12.14  minimum=3.12.14   measured=3.12.14   MATCH
  cobc (GnuCOBOL)            required=3.1.2.0  minimum=3.1.2     measured=3.2.0     DEVIATION
  git                        required=2.51.0   minimum=2.43.7    measured=2.51.0    MATCH
  pip (inside the venv)      required=26.2.1   minimum=26.2.1    measured=26.2.1    MATCH
  the minimum column is the lowest version make verify-env accepts, because a
        lower one carries a published advisory with an available fix; a
        measured version at or above it and different from the required value
        is reported as a deviation, and one below it fails the stage in every
        mode

pinned apt specifications, availability in the configured suites:
  gnucobol3=3.1.2-5.1ubuntu1      apt-get download: version not found
  git=1:2.51.0-1ubuntu1           apt-get download: available, and the
        installed version
  python3.12=3.12.3-1ubuntu0.15   apt-get download: no package matches the name
  python3.12-venv=3.12.3-1ubuntu0.15  apt-get download: no package matches the name
  installed instead: gnucobol3 3.2-4,
        /usr/local/bin/python3.12 built from source
  the two apt Python specifications package interpreter release 3.12.3, below
        the accepted interpreter minimum of 3.12.14, and no apt python3.12
        package reaches that minimum in any configured suite; the interpreter
        measurement above governs and these pins record the packaging alone

direct Python dependency pins (required == measured for every entry):
  dbt-core               required=1.12.3     measured=1.12.3     MATCH
  dbt-duckdb             required=1.11.0     measured=1.11.0     MATCH
  dbt-redshift           required=1.11.1     measured=1.11.1     MATCH
  duckdb                 required=1.5.5      measured=1.5.5      MATCH
  redshift-connector     required=2.1.16     measured=2.1.16     MATCH
  boto3                  required=1.43.74    measured=1.43.74    MATCH
  botocore               required=1.43.74    measured=1.43.74    MATCH
  moto                   required=5.2.2      measured=5.2.2      MATCH
  PyYAML                 required=6.0.3      measured=6.0.3      MATCH
  jsonschema             required=4.26.0     measured=4.26.0     MATCH
  pip check: no broken requirements found
  import surfaces exercised: 12 of 12
  dbt: Core 1.12.3 with duckdb 1.11.0 and redshift 1.11.1 adapters
  transitive resolution of note: sqlparse 0.6.0, the release that fixes the
        advisories the earlier 0.5.5 resolution carried; the two dbt
        distributions above are the lowest releases whose declared
        constraints admit it
  hash-pinned closure: modernization/requirements-lock.txt carries 106
        distributions, each with its sha256, and covers all 10 direct pins;
        pip install --require-hashes -r modernization/requirements-lock.txt
        reproduces this closure exactly
  security audit of the resolved closure: pip-audit 2.10.1 over the installed
        distributions reported 0 known vulnerabilities, and over
        modernization/requirements.txt reported none found (measured
        2026-08-23, PyPI advisory data of that date)

compiler dialect resolution (measured 2026-08-21T02:30:30Z, cobc 3.2.0):
  COB_CONFIG_DIR      /usr/share/gnucobol/config, the directory cobc --info
        reports as its own; modernization/harness/run_harness.sh pins it, and
        pins COB_CFLAGS, and leaves COB_RUNTIME_CONFIG unset, before the first
        compile of a run, reporting any ambient value it replaced
  dialect chain of the mandated -std=ibm (file, kind, sha256):
    ibm.conf         dialect  9220624ee67056073268e58bd16fdf819afd1e17f249c4603c06270717a3995c
    ibm-strict.conf  dialect  e1eebcdcebae06278a533875e20e7cac809465dac8ba2211d7d41dd3b1a25ce6
    ibm.words        words    e13017258080c6a41f596724aec3d4aa6c65082368d3dd94d1bbb6344ab8054d
    lax.conf-inc     dialect  f813b082f706e369eac184f653b0af82566f88448af277acc2c9c149d7454f3d
  numeric-store keys the harness depends on (key, resolved value, file):
    binary-size        2-4-8        ibm-strict.conf
    binary-truncate    no           ibm-strict.conf
    binary-byteorder   big-endian   ibm-strict.conf
    hostsign           yes          ibm-strict.conf
    defaultbyte        0            ibm-strict.conf
  binary truncation in force        yes, from the mandated -fbinary-truncate,
        over the binary-truncate the chain resolves. Measured on a probe
        performing the SUBTRACT of [base/src/lgapdb01.cbl:339-340] into a
        PIC S9(4) COMP item at EIBCALEN=32500: 32348 without the option, 2348
        with it, and 32500 unchanged in the PIC S9(4) COMP-5 EIBCALEN item
  COB_CFLAGS pinned to             -std=c17 -finline-functions -pipe
        -Wdate-time -Wno-unused -fsigned-char -Wno-pointer-sign
        -D_FORTIFY_SOURCE=3: the C options cobc --info reports, with the
        _FORTIFY_SOURCE definition its host toolchain states twice reduced to
        the one that takes effect. Measured: 16 host redefinition warnings
        before the pin, 0 after it, and a COBOL warning still reported

capability probes on the measured runtimes (command, observed):
  cobc -m -std=ibm -fbinary-truncate -ffold-copy=LOWER -ext cpy
        exit 0, module produced
  cobc -x -std=ibm -fbinary-truncate -ffold-copy=LOWER -ext cpy
        exit 0, driver produced
  module lookup through COB_LIBRARY_PATH        exit 0 for an upper-case -o
        module name and for a file-named module with COB_LOAD_CASE=LOWER
  cross-module EXTERNAL storage                 EIBCALEN=+32500,
        SQLCODE=-0000000530, HC-POL-PRESENT=Y, HC-POL-PAYMENT=+0000001750
  88-level condition on EXTERNAL storage        HC-VSAM-CAPTURED true
  COPY of the level-03 COMMAREA copybook inside an enclosing 01 group
        CA-REQUEST-ID=01ACOM
  COPY of the Db2 host-variable copybook        DB2-PAYMENT=001750
  named normal-response constant                DFHRESP-NORMAL=+0000000000
  generated fixed-format text                  0 lines beyond column 72
  git rev-parse --show-toplevel                exit 0
  git status --porcelain -- base/               exit 0
  git diff --name-only HEAD                    exit 0
  git status --porcelain                       exit 0
  git lfs env                                  exit 0

verdict: one system runtime version deviates from its required value, cobc
        3.2.0 against the required 3.1.2.0, and it is above the accepted
        minimum; three of the four pinned apt specifications are not present in
        the configured suites; every interpreter, pip and Python package pin
        matches, the resolved closure carries no known vulnerability, the
        dialect the harness compiles under is resolved and recorded, and every
        probed capability the harness, the record builder and the read-only
        gate rely on is present
END runtime-version-report
