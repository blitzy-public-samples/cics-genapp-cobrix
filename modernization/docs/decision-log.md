# Decision Log — GenApp Policy-Issue Canonical Warehouse Bridge

> **Status — validated against local substitute, not AWS.** Every result this log refers to was produced on the
> local-substitute branch: a moto S3 endpoint in place of Amazon S3 and DuckDB in place of Amazon Redshift. The formal
> AWS diff requirement is OPEN and this has not yet happened. Nothing in this document may be read as closing it, and
> no row here records a provisioned AWS resource.

This log is the single rationale source for the whole `modernization/` tree. Every other artifact in that tree — Python,
COBOL, SQL, YAML, JSON, shell and Markdown — carries operational comments only and defers its "why" to a row below, so a
decision absent from this file is undocumented. Row IDs are stable and citable: `D-01` through `D-115` are assigned
once, 115 rows in all, sibling artifacts cite them by row subject, and no ID is renumbered or reused. Rows `D-01`
through `D-78` are the decisions of the build; rows `D-79` onward are the decisions taken when the findings of the final
QA pass were closed, each standing in the section its subject belongs to — Section F holds the test-infrastructure
remediation, and the remainder stand beside the build decisions they extend in Sections B, C and D. A row that a later
decision replaces is marked **SUPERSEDED BY** the row that replaces it and is kept in place, so a citation of it still
resolves and the change of course is visible rather than erased.

The five authorized COBOL and copybook sources — `base/src/lgapol01.cbl`, `base/src/lgapdb01.cbl`,
`base/src/lgapvs01.cbl`, `base/src/lgcmarea.cpy` and `base/src/lgpolicy.cpy` — are read-only and byte-identical to their
state before this work. Every claim below about the existing system carries an inline `[base/src/<file>:<locator>]`
citation, and no citation in this file points outside those five files. Diagrams belong to
`modernization/docs/architecture.md`; this file draws none and references figures by name instead.

Every row carries its ID, then the four things this record must state, one per column: what was decided, what
alternatives existed, why this choice was made, and what risk it carries. A sixth column names the evidence behind the
row and the artifacts it governs.

## Section A — Scope, interpretation and target selection

| ID | Decision | Alternatives considered | Why this choice | Risk | Evidence and references |
|---|---|---|---|---|---|
| D-01 | Modify no pre-existing repository file. Every deliverable of this work is a new file beneath `modernization/`, and the five named artifacts are read-only. | (a) Read "zero files touched outside the named five" literally as "create nothing", producing no deliverable at all. (b) Host the deliverables inside an existing repository file rather than a new tree. | A literal no-new-file reading makes the requested deliverables impossible to produce; option (b) would break the very guard it is meant to protect. A separate top-level tree makes the read-only boundary explicit and lets a nested ignore file cover only generated bridge artifacts. | The new tree must be self-contained, and the boundary is only as strong as its enforcement. Mitigated by machine verification: the read-only gate records the five SHA-256 hashes, requires `git status --porcelain -- base/` to be empty and requires `git diff --name-only HEAD` to hold no tracked modification. It runs before any generated output, after each harness stage and as the final gate. | `modernization/validation/verify_readonly.sh`; `modernization/.gitignore`; the five sources listed in the header. |
| D-02 | Treat the request's field language as **17 logical field entries** resolving to **16 distinct runtime business values**, projected into **18 source-derived canonical column instances** — policy number and policy type appear on both relations — plus one warehouse-assigned `source_system_key` on each relation, giving **20 canonical column instances** in total across 11 columns of `canonical.issued_policy` and 9 of `canonical.preissued_rating`. | (a) Take the phrase "11 fields" literally and build 11 columns, dropping the six amounts or the request/return identifiers. (b) Invent one target column for every declaration encountered, including Db2-side declarations that no statement reads. | The 17 entries resolve as 6 premium/payment entries plus 11 policy/request entries, of which 15 are active, 1 is derived and 1 is declaration-only. The declaration-only entry is `DB2-POLICYNUMBER PIC 9(10)` [base/src/lgpolicy.cpy:44]: it is referenced nowhere in the Db2 program — the POLICY insert supplies the literal `DEFAULT` for the POLICYNUMBER column [base/src/lgapdb01.cbl:279] — and the recovered key is carried by `CA-POLICY-NUM` [base/src/lgcmarea.cpy:35] through the separate binary intermediate `DB2-POLICYNUM-INT` [base/src/lgapdb01.cbl:117,307-311]. It therefore earns a lineage entry, not a column. Option (b) would fabricate columns with no runtime value behind them. | The arithmetic must stay consistent across three artifacts or the traceability matrix will not reconcile: the `counts` block of the field map, the enforced dbt contracts and this row. Any future field must be added to all three in one change. | `modernization/extraction/copybook_field_map.yml` (`counts`: 17 logical entries, 15 active, 1 derived, 1 declaration-only, 16 runtime values, 18 source-derived instances, 11 and 9 total columns); `modernization/dbt/genapp_rqi/models/marts/canonical/_canonical__models.yml`. |
| D-03 | Allow `source_system_key` as the sole warehouse-assigned canonical column with no COBOL origin. It sits on both relations and forms the first element of the natural key. | (a) Omit it, which breaks the stated extensibility requirement that rows from a future source system slot into the same tables without structural change. (b) Synthesise it from a COBOL item. | The discriminator is an explicit user requirement. Option (b) is impossible rather than merely undesirable: no item in the COMMAREA or the Db2-side declarations identifies the source system, because the source system is the whole program chain and not a value it carries. | It is the one canonical column that cannot be diffed against a source value, so the comparison gate has no independent oracle for it and instead checks it against the configured run value. A misconfigured run value would therefore be silently accepted as correct. | `modernization/validation/diff_harness_vs_warehouse.py`; `modernization/extraction/copybook_field_map.yml`; the `source_system_key` entries of `_canonical__models.yml`. |
| D-04 | Retain the stated ±0.01 threshold as the pass/fail contract for the six amounts, and report any **non-zero in-tolerance** delta as an anomaly that still passes. | (a) Narrow the threshold to exact equality, which the representation would support. (b) Widen it. | The six amounts are unsigned display numerics with no implied decimal — `PIC 9(6)` for payment and motor premium and `PIC 9(8)` for the four commercial premiums [base/src/lgcmarea.cpy:43,73,85,87,89,91] — and they reach Db2 through plain `MOVE` into binary host variables [base/src/lgapdb01.cbl:92,100,103,105,107,109], so exact equality is what the chain produces. Narrowing the threshold would nonetheless change the stated acceptance contract, and widening it is unjustified by any representation in the source. | A genuine non-zero delta can pass the gate. Mitigated by classifying it separately: an in-tolerance delta is recorded as an anomaly, counted in the summary and printed in its own report section rather than folded into the pass count. | `AMOUNT_TOLERANCE` and `STATUS_PASS_IN_TOLERANCE` in `modernization/validation/diff_harness_vs_warehouse.py`; `amount_tolerance_abs` in `modernization/extraction/copybook_field_map.yml`. |
| D-05 | Re-run the precondition gate before execution rather than trusting an earlier observation, and select the local-substitute branch only if the gate still fails. Probe, never provision. | (a) Assume the earlier environment observation still holds and hard-code the local branch. (b) Drop real-target support and build only for the local substitute. | The requirement is to use real AWS whenever both targets are available, so the branch has to be a measured property of the run and not a build-time constant. Option (b) would make a later real-target run a code change instead of a configuration change, which D-09 exists to prevent. | Branch selection has to be recorded in the evidence before extraction begins, or a later reader cannot tell which target produced a result. Mitigated by carrying the status label of this document's header next to every claimed result. | `modernization/dbt/genapp_rqi/profiles.example.yml` (target selection through `DBT_TARGET`); `modernization/landing/land_to_s3.py` (endpoint selection); `modernization/validation/artifacts/` status labels. |
| D-06 | Translate read-only copies of the three programs into `modernization/harness/build/**` and compile the copies. Never preprocess a source file in place. | (a) Preprocess the originals, which would violate byte-identical preservation. (b) Compile the untouched sources directly. | Option (b) cannot work: the programs carry 36 `EXEC CICS` blocks and 11 `EXEC SQL` blocks — 9 and 0 in `lgapol01.cbl`, 20 and 11 in `lgapdb01.cbl`, 7 and 0 in `lgapvs01.cbl` — and reference five EIB fields the compiler does not supply [base/src/lgapol01.cbl:88-101; base/src/lgapdb01.cbl:151-168; base/src/lgapvs01.cbl:97,142-146]. Both constraints — an unchanged source and an executable chain — are satisfiable only by generating copies. | Generated code can drift from the originals. Mitigated by regenerating every copy from the sources on every run, by never committing the build tree and by re-running the SHA-256 baseline gate around the generation step. | `modernization/harness/translate.py`; `modernization/harness/translation-rules.md`; `modernization/harness/build/` is ignored by `modernization/.gitignore`. |
| D-07 | Proceed on the local-substitute branch — moto for the S3 API surface, DuckDB for the warehouse — and keep the formal AWS diff **OPEN**. | Block the build until credentials arrive, delivering nothing meanwhile. | The measured environment resolves no credentials and no region, has no configured bucket, no Redshift cluster or workgroup, no `~/.aws` directory and no AWS CLI. Blocking would forfeit the transform logic, the harness evidence and the documentation, all of which are target-independent. The instruction is explicitly not to block on unavailable AWS access. | Local results could be mistaken for closing the formal AWS diff requirement. They do not, and must never be presented as though they do: the local run exercises the transform logic only. Mitigated by the exact status label carried in this and every other human-facing deliverable, and by keeping the AWS disposition OPEN until every real-target gate passes. | The status header of this document; `modernization/validation/validation-evidence.md`; `modernization/docs/project-guide.md`. |
| D-08 | Change no source length constant. Always emit the full 32,500-character sample record, and validate that the applicable amount bytes are numeric before landing. | (a) Correct the constant, which is forbidden because the file is read-only. (b) Emit a 165-byte record matching what the program validates, which would truncate the very premium field under test. | The declared constants are `WS-MOTOR-LEN +65` [base/src/lgpolicy.cpy:21] and `WS-FULL-MOTOR-LEN +137` [base/src/lgpolicy.cpy:26], so the Db2 program requires 28 + 137 = 165 bytes for the motor route [base/src/lgapdb01.cbl:182,195]. The overlay actually ends at byte 177: starting at byte 101 it runs MAKE 101-115, MODEL 116-130, VALUE 131-136, REGNUMBER 137-143, COLOUR 144-151, CC 152-155, MANUFACTURED 156-165, `CA-M-PREMIUM` 166-171 and `CA-M-ACCIDENTS` 172-177 [base/src/lgcmarea.cpy:65-75] — a 77-byte overlay and a 12-byte shortfall exactly equal to premium (6) plus accidents (6). A full-length record removes the ambiguity without touching the source. | A caller honouring only the declared length would leave the premium bytes unset and the chain would still accept the request. The full-length sample removes that ambiguity for this exercise, but the latent source defect remains and any future caller inherits it. | `modernization/extraction/build_sample_commarea.py`; `modernization/extraction/sample_input/README.md`; `modernization/extraction/extract_commarea.py` numeric validation. |
| D-09 | Confine engine-specific behavior to raw loading — a `COPY` for Amazon Redshift, a DuckDB insert locally — and keep every dbt model file byte-identical between the two targets. | (a) Branch inside the model SQL on the active adapter. (b) Maintain two model sets, one per target. | The acceptance requirement is that the same dbt models run unmodified against the real target later, so the adapter boundary has to sit below dbt. Both alternatives would make a Redshift run a model edit, which is precisely what the requirement forbids. | The two loaders must produce an identical raw shape or the shared models would silently diverge by target. Mitigated by deriving column names, order and widths in both loaders from one contract, `modernization/landing/landing-schema.json`, and by asserting the loaded shape rather than assuming it. | `modernization/landing/load_redshift.sql`; `modernization/landing/load_local.py`; `modernization/landing/landing-schema.json`; the model files under `modernization/dbt/genapp_rqi/models/`. |
| D-10 | Rely on Redshift automatic table optimization. Declare no `DISTKEY`, no `SORTKEY` and no `DISTSTYLE` on any relation. | Guess explicit distribution and sort keys from the shape of the schema. | No workload evidence exists — there is no stated throughput, latency or cost target and no query history — and official guidance favours automatic optimization in exactly that situation. Physical partitioning of the S3 prefix is a separate concern from warehouse table design and is handled there instead. | A future measured workload may justify explicit keys, and until then the physical layout is not tuned. The revisit trigger is the first measured query workload against the real target; a change then needs its own row here rather than a silent edit. | `modernization/warehouse/ddl/02_raw_genapp_policy_issue.sql`; the mart models under `modernization/dbt/genapp_rqi/models/marts/canonical/`; `modernization/landing/partition-layout.md`. |
| D-11 | Build exactly two canonical relations, `canonical.issued_policy` and `canonical.preissued_rating`. Add no source registry, no extra provenance column, no rating formula, no rating factor, no derived-factor column, no Quote model, no Loss model and no commission field. | (a) Add a third registry relation to hold source-system metadata. (b) Synthesise a premium formula so a rating column could be populated. | The directive not to construct, infer or backfill a rating formula is explicit, and the source measurement agrees with it: the three programs contain **zero** `COMPUTE`, `MULTIPLY` and `DIVIDE` statements. The only arithmetic in the corpus is 6 `ADD` and 1 `SUBTRACT`, every one of them on a length constant [base/src/lgapol01.cbl:109; base/src/lgapdb01.cbl:182,187,191,195,199,339-340]. The six amounts are passthroughs, moved by plain `MOVE` [base/src/lgapdb01.cbl:265,445,489,491,493,495]. Option (a) is unnecessary because `source_system_key` on both relations already carries the discriminator (D-03). | An analyst may expect a rating or factor column and read its absence as an omission. It is not: the absence of executable rating logic is a measured finding of this exercise and is recorded as such in the extraction specification, the lineage document and the project guide. | The measurement above; `modernization/extraction/extraction-spec.md`; `modernization/docs/field-level-lineage.md`; `_canonical__models.yml`, the only declaration of the canonical column set. |
| D-12 | Exclude the four commercial peril fields `CA-B-FirePeril`, `CA-B-CrimePeril`, `CA-B-FloodPeril` and `CA-B-WeatherPeril` from the canonical amount columns. | Treat them as amounts, on the strength of their adjacency to the premium items and their appearance in the same insert. | They are `PIC 9(4)` codes, not amounts [base/src/lgcmarea.cpy:84,86,88,90]; the requested amounts are the adjacent `PIC 9(8)` premium items [base/src/lgcmarea.cpy:85,87,89,91]. The insert carries both, one pair per peril [base/src/lgapdb01.cbl:534-541], so adjacency alone cannot decide the question and the PICTURE does. | No material risk to the canonical model. One consequence is worth stating — **commercial peril operands accepted by the harness stubs**: the named chain cannot execute without them, so their presence in the capture, and the capture slots that hold them, are expected and are not a scope leak. | `modernization/harness/stubs/sql_insert_commercial.cbl`; `modernization/harness/statement_map.yml` (`insert_commercial` host list); `modernization/extraction/copybook_field_map.yml`. |
| D-13 | Keep AWS Glue, orchestration and every future data domain as documented proposals only, confined to **Figure 3 — AFTER (PROPOSED, NOT BUILT): AWS-Native Multi-RQI Warehouse** in `modernization/docs/architecture.md`. Build none of them. | (a) Build them, exceeding the stated scope and the AWS provisioning cap. (b) Omit them entirely, losing the context a reader needs to place the built bridge. | They exceed the built scope while remaining necessary context for how the bridge is meant to grow. Naming them in one figure that is explicitly labelled as not built keeps that context available without implying delivery. | A reader could mistake proposal for delivery. Mitigated three ways: the figure name carries "PROPOSED, NOT BUILT", the figure's node labels and legend mark unbuilt services and future domains distinctly, and it is the only figure in that document carrying no delivered-or-pending component marker. | **Figure 3 — AFTER (PROPOSED, NOT BUILT): AWS-Native Multi-RQI Warehouse** in `modernization/docs/architecture.md`; `modernization/docs/project-guide.md`. |
| D-14 | Leave orchestration an explicitly flagged **open decision** between AWS Glue jobs, AWS Step Functions and Apache Airflow. Record no preference. | (a) Resolve it now by naming one of the three as the intended orchestrator. (b) Drop the question from the documentation altogether, leaving a reader to assume the pipeline is run by hand. | An open decision is what was asked for rather than a resolved choice, and nothing in the built scope discriminates between the three: there is no schedule, no dependency graph beyond a single linear path and no operational history to argue from. Recording a preference would present an unevidenced choice as a finding, and dropping the question would leave a real gap invisible. | The choice has to be made before any production schedule exists, and leaving it open means no runbook can describe scheduled operation. The trigger for closing it is the first requirement for unattended, repeated execution; closing it needs its own row here. | The orchestration node of **Figure 3 — AFTER (PROPOSED, NOT BUILT): AWS-Native Multi-RQI Warehouse** in `modernization/docs/architecture.md`; `modernization/docs/project-guide.md`. |

## Section B — Decisions embedded in sibling artifacts

Rationale may not live in code comments, so every decision physically encoded in another artifact of the tree is recorded
here instead. Each row below was verified against the artifact as it now stands, not against the intent it was planned
with.

| ID | Decision | Alternatives considered | Why this choice | Risk | Evidence and references |
|---|---|---|---|---|---|
| D-15 | Encode the policy insert as a hard prerequisite of every product insert, and of the commercial insert in particular, as an ordering constraint the harness checks at run time rather than a convention. | Allow the stubs to be called in any order and rely on the translated program's own `PERFORM` sequence to get it right. | The commercial insert supplies `:CA-LASTCHANGED` as its `RequestDate` value [base/src/lgapdb01.cbl:525], and `CA-LASTCHANGED` is populated only by the read-back that follows the policy insert [base/src/lgapdb01.cbl:315-321]. The source order is `PERFORM INSERT-POLICY` first [base/src/lgapdb01.cbl:219] and product routing after [base/src/lgapdb01.cbl:223-241], so the constraint is a property of the source and worth asserting rather than assuming. | A reordering would yield a blank `RequestDate` and a silently wrong capture that still returned `00`. Mitigated by making the violation loud: the ordering entry names the witness items and the run fails when the observed ordinals disagree. | The `lastchanged_before_commercial`, `policy_first_captured`, `policy_before_identity` and `identity_before_lastchanged` entries of the `execution_order` block in `modernization/harness/statement_map.yml`. |
| D-16 | Keep one statement map covering the `INCLUDE` blocks and the DML blocks alike, rather than one map per block kind. | Split the map: one file for copybook includes, one for data-manipulation statements. | All eleven SQL blocks of the Db2 program are translated by the same rule engine and are validated against the same copybook and the same driver order table, so splitting the map would duplicate the cross-checks and allow the two halves to drift. One map also makes the census — three includes and eight DML blocks — checkable in one place [base/src/lgapdb01.cbl:75-77,124-126,135-137,268-545]. | A single file grows large and mixes two concerns. Mitigated by a strict scope boundary: the map holds SQL-block-to-harness-construct mapping only, while warehouse column mapping, runtime status, COMMAREA offsets and the landing record live in the field map. | `modernization/harness/statement_map.yml`; `modernization/extraction/copybook_field_map.yml`. |
| D-17 | Normalise both endowment INSERT branches to one **single-superset SQL-INSERT-ENDOWMENT call**: one stub taking one 9-item superset `USING` list, ordered exactly as the with-varchar branch declares it. The stub ignores the padding argument when the varying length is not positive. | (a) Emit two stubs, one per branch. (b) Omit the varchar branch and translate only the shorter insert. | One call shape keeps the generated code faithful to both branches without duplicating a module, and the superset is well defined because the shorter branch's operand list is a prefix of the longer one [base/src/lgapdb01.cbl:346-366,368-386]. Option (b) would drop a real source path from the translation. | The superset must stay in the with-varchar branch's declared order, or the stub would bind the wrong operand to the wrong column. Mitigated by the translator failing when the mapped `USING` list disagrees with the map, and by the map recording the order explicitly. | `modernization/harness/stubs/sql_insert_endowment.cbl`; the `insert_endowment_varchar` and `insert_endowment_novarchar` entries of `modernization/harness/statement_map.yml`; `modernization/harness/translate.py`. |
| D-18 | Leave the **endowment route not executed**: it is compiled, but no case selects it and no endowment sample is authored. Every case asserts the endowment capture group absent. | (a) Add an endowment case for route coverage and accept whatever the truncated length produces. (b) Author an endowment sample short enough to keep the subtraction in range, which no fixture of this harness uses and which D-08 rejects for the motor route on the same grounds. | A full-length COMMAREA drives `SUBTRACT WS-REQUIRED-CA-LEN FROM EIBCALEN GIVING WS-VARY-LEN` [base/src/lgapdb01.cbl:339-340] to 32,348 for that route — 28 + `WS-FULL-ENDOW-LEN` 124 = 152, and 32,500 less 152 = 32,348 — which exceeds both the four-digit decimal capacity of the `PIC S9(4) COMP` receiver [base/src/lgapdb01.cbl:71] and the 3,900-character length of the `WS-VARY-CHAR` item it then reference-modifies [base/src/lgapdb01.cbl:72]. Measured on a probe, the stored value is 32,348 without binary truncation and 2,348 with it, so under the compile options of D-23 the route survives but the reference-modification length it uses is a truncation artifact rather than the padding length the source intends. Exercising it would characterise the artifact rather than the chain, and a deliberately shortened sample would reproduce for the endowment route exactly the truncation of an under-length record that D-08 rejects for the motor route. | The endowment route is compiled and reachable but unexercised, so no endowment behaviour is demonstrated and no endowment canonical value is claimed anywhere in this work. Stated plainly rather than left implicit. | The case table of `modernization/harness/run_harness.sh`; the truncation probe in `modernization/validation/artifacts/runtime-versions.txt`; `modernization/harness/stubs/sql_insert_endowment.cbl`. |
| D-19 | Assign the Db2 identity and the read-back timestamp from deterministic harness seeds: one identity seed for the first case of the table with each later case taking the next value, and one fixed 26-character timestamp for every case. | Derive them from a random source or from the wall clock, as a real Db2 would. | Reproducibility: two runs of the same selection over one unchanged tracked state must produce comparable evidence, and the comparison gate needs a value it can predict. The two success cases must also receive **distinct** identities, or the uniqueness assertions on the natural key `(source_system_key, policy_number)` would pass vacuously with both samples loaded. | The seeds are harness artifacts and not source behaviour: the real chain lets Db2 allocate the key through `IDENTITY_VAL_LOCAL()` [base/src/lgapdb01.cbl:307-311] and reads the timestamp back from the row [base/src/lgapdb01.cbl:315-321]. No conclusion about real key allocation may be drawn from a harness identity. Mitigated by validating the seed's shape and by reserving one identity per table row so a partial selection cannot collide. | The seed handling in `modernization/harness/run_harness.sh`; `modernization/harness/stubs/sql_set_identity.cbl`; `modernization/harness/stubs/sql_select_lastchanged.cbl`. |
| D-20 | Use **deterministic failure injection through shared harness state** so the preserved return-code contract is demonstrated rather than only described. The case table drives twelve cases covering `00` on the motor, commercial and house routes, `70`, `90`, `80`, `98`, `99` and both abend codes. | Run only the two success cases and document the failure codes from the source alone. | The return convention is part of the interface this work is required to preserve and report, and a code that is only read out of the source is not evidence that the translated chain reproduces it. Injection through the shared capture group needs no source change: the stub reads the injected value and the translated program's own `EVALUATE` does the rest [base/src/lgapdb01.cbl:290-305]. | Injected SQLCODEs and VSAM responses are harness inputs, so a case proves the chain's reaction to a condition and not that the condition can arise in production. Mitigated by naming the injected value in each case row and by carrying it in the retained per-case log. | The case table of `modernization/harness/run_harness.sh`; the injection points in the five `modernization/harness/stubs/sql_insert_*.cbl` modules and `modernization/harness/stubs/cics_write.cbl`; `modernization/harness/copybooks/hcapture.cpy`. |
| D-21 | Accept two paths as unexercised and say so: the endowment route per D-18, and the second diagnostic link of the entry program. Record also the **abend sites unexercised by the two passing samples** and the **unexercised diagnostic paths** of those two cases: both are reached only by the injected cases of D-20. Claim no coverage beyond that. | Force them: author an endowment case, and patch the generated entry program so the second diagnostic link becomes reachable. | The second diagnostic link is unreachable by construction, not by omission. The diagnostic paragraph links once unconditionally and a second time only under `IF EIBCALEN > 0` [base/src/lgapol01.cbl:149-166], while the only `PERFORM` of that paragraph sits inside `IF EIBCALEN IS EQUAL TO ZERO` [base/src/lgapol01.cbl:98-102]. Reaching it would require changing the generated copy's control flow, which would stop it from characterising the source. | Coverage is genuinely incomplete for those two paths, and this row is the only place that fact is recorded. Every other return code and both abend codes are exercised per D-20, so the gap is bounded and named rather than general. | `modernization/harness/run_harness.sh` (the diagnostic-link counts it reports per case); `modernization/harness/stubs/cics_diag_link.cbl`; D-18 above. |
| D-22 | Preserve the source's halfword receivers exactly as declared in the generated copies. Widen nothing. | Widen `PIC S9(4) COMP` items to `PIC S9(9) COMP` so length arithmetic could not truncate. | Widening would change observed behaviour, and observed behaviour is the whole point of the harness: the translated copy exists to characterise the source, so a receiver that truncates must go on truncating. | Length arithmetic can truncate, and D-18 is the concrete instance. A second consequence: the accumulation described in D-24 is also a property of these narrow, never-reinitialised items and is preserved with them. | `modernization/harness/translate.py` (no data-division widening rule); `base/src/lgapdb01.cbl:66,71`; `base/src/lgapol01.cbl:60`. |
| D-23 | Compile every module and the driver under **the fixed compile options including `-fbinary-truncate`** — `-std=ibm -fbinary-truncate -ffold-copy=LOWER -ext cpy` — adding `-fbinary-truncate` to the option list the specification names, and accept only allow-listed additions from the environment. | (a) Use the specification's option list unchanged, without binary truncation. (b) Let the operator pass arbitrary compiler options. | The resolved dialect chain of `-std=ibm` sets `binary-truncate no`, which would let the endowment length subtraction store 32,348 in a four-digit decimal item and abort the run on an out-of-range reference modification. Truncation restores the decimal-capacity semantics the declaration implies and keeps the route survivable, which is why it is mandated rather than optional. Option (b) would make the compile environment unreproducible. | Adding an option is a deviation from the literal option list of the specification, recorded here for that reason. It changes numeric storage behaviour for every binary item in the run, so the compile log records the resolved dialect keys and the measured effect rather than asserting it. | The mandated options and the option allow-list in `modernization/harness/run_harness.sh`; `COBFLAGS` in `modernization/Makefile`, so the second compile check of the `compile` target carries the same four options; the dialect resolution and truncation probe in `modernization/validation/artifacts/compile.log` and `modernization/validation/artifacts/runtime-versions.txt`. |
| D-24 | Run one operating-system process per case, each making one chain call, and preserve the source's cross-call accumulation rather than working around it. | Reuse one process for several cases, or re-initialise the length accumulator in the generated copies so repeated calls behave like the first. | `WS-REQUIRED-CA-LEN PIC S9(4) VALUE +0` is never re-initialised [base/src/lgapol01.cbl:60; base/src/lgapdb01.cbl:66] and each program adds to it before comparing it with the received length [base/src/lgapol01.cbl:109; base/src/lgapdb01.cbl:182-199], so a second call in one process compares against twice the requirement. Re-initialising it in the copy would hide a real property of the frozen source; one process per case exposes it without touching the source. | The accumulation is a latent source behaviour any repeat caller inherits: a request that returns `00` at a short length returns `98` from the second call in the same process onward. A host reusing these modules has to cancel them between calls to reproduce the first result. That consequence belongs to the source, not to the harness. | `modernization/harness/run_harness.sh` (one driver invocation per case, and the property it documents); `modernization/harness/driver.cbl`. |
| D-25 | Take an exclusive per-checkout lock for the whole harness run, so two invocations in one checkout serialise instead of overlapping, and give up with a distinct exit status when the wait runs out. | (a) No lock, relying on the operator not to overlap runs. (b) A host-wide lock. | The harness regenerates one shared build tree and republishes one evidence set per checkout, so two concurrent runs would interleave writes into both. A per-checkout lock is the narrowest scope that prevents that; a host-wide lock would serialise unrelated checkouts that share nothing. | A run that cannot take the lock does no work, which is a visible failure rather than a silent one, and a stale lock would block a legitimate run. Mitigated by a bounded wait, a distinct exit status, and leaving every shared path untouched when the wait expires. | The lock handling and wait bound in `modernization/harness/run_harness.sh`. |
| D-26 | Hold one **shared EXTERNAL harness state**: the EIB fields and the whole capture group are declared as `EXTERNAL` storage in two harness copybooks, so the driver, the three translated programs and the twelve stubs all address one instance. | (a) Pass the capture area as an extra parameter through every call. (b) Give each module its own copy and reconcile afterwards. | Option (a) would change the parameter contract of the translated programs, which this work must preserve — each is entered with the shared COMMAREA alone [base/src/lgapol01.cbl:70-77]. Option (b) cannot work at all: a stub's capture has to be visible to the driver that started the chain. `EXTERNAL` storage gives one instance without touching any call signature. | Shared mutable state is order-sensitive and any module could overwrite another's item. Mitigated by strict ownership: each stub writes only the items its own comment enumerates, the group carries no `VALUE` clause and is initialised once by the driver, and the translator fails when a stub's declared surface disagrees with the copybook. | `modernization/harness/copybooks/dfheiblk.cpy`; `modernization/harness/copybooks/hcapture.cpy`; `modernization/harness/driver.cbl`. |
| D-27 | Carry one **shared event-sequence ordering witness and order guard**: a monotonic sequence in the capture group stamps every captured statement with an ordinal, so the observed order is a recorded value rather than an inference. | Record only that each statement happened, and infer order from the source's control flow. | Order is exactly what D-15 needs to assert, and an inference from control flow cannot detect a stub called out of order. One shared counter makes the order a witnessed fact: an entry declares the ordinals it expects and the run fails when the observed values disagree. | A shared counter is itself shared state, and a module that forgot to stamp would corrupt the sequence for everything after it. Mitigated by the translator reading both the copybook and the ordering block on every run and failing when the two disagree on any member's prerequisite, its declared ordinals, or an ordinal item declared but accounted for nowhere. | The `HC-ORDER` group and the ordinal items of `modernization/harness/copybooks/hcapture.cpy`; the `execution_order` and `capture_ordinals` blocks of `modernization/harness/statement_map.yml`. |
| D-28 | Give every capturing stub one **uniform stub-side capture-order guard**: it tests its own prerequisite ordinals and, on a violation, sets the shared violation flag and names itself as the offending statement. | (a) Centralise the check in the driver after the chain returns. (b) Give each stub a bespoke guard shaped to its own statement rather than one shared pattern. | A stub is the only place that knows the state at the moment it ran; a check after the fact sees only the end state and cannot name the statement that broke the order. A uniform guard also makes the eight constrained stubs reviewable against one pattern instead of eight. | Uniformity is only as good as its enforcement, and a stub with a wrong guard would report a false verdict. Mitigated by the translator validating each member's guard against the ordinals declared for it, and by the four unconstrained stubs neither reading nor writing the violation items so an abend or a diagnostic link cannot disturb a verdict. | The order-guard sections of the twelve modules under `modernization/harness/stubs/`; `modernization/harness/copybooks/hcapture.cpy`; `modernization/harness/statement_map.yml`. |
| D-29 | Rewrite every `EXEC CICS RETURN` as an inline `GOBACK` in the generated copy. There is **no called RETURN stub**. | Emit a call to a `CICS-RETURN` stub, for symmetry with the other emulated commands. | A called stub would return control to the statement after the call and execution would resume, so a program that the source ends at that point would carry on into later business logic. Every return site in the chain is a real exit [base/src/lgapol01.cbl:115,126; base/src/lgapdb01.cbl:205,212,250,298,303; base/src/lgapvs01.cbl:146], so a symmetric stub would be actively wrong. | An inline rewrite is invisible in the capture: no record says "the program returned here". Mitigated by the translator recording the rewrite per site in its report, with the note that control returns to the caller at that point. | Rule R7 of `modernization/harness/translate.py`; the per-site entries of `modernization/validation/artifacts/translation-report.json`. |
| D-30 | The **called abend stub records and returns**: the translated program moves the site's literal code into an item of its own, calls a one-parameter stub that records it, and the **caller** then issues `GOBACK` immediately. The stub terminates nothing. | (a) Have the stub end the run unit, matching the real command's effect. (b) Ignore the abend sites. | Option (a) would end the process inside a called module and lose the capture file and the post-chain record the driver still has to write, so the case would fail with no evidence of why. Emitting the `GOBACK` in the caller reproduces the observable effect — no later business step runs — while leaving the evidence intact. Option (b) would silently continue past a real failure. | The emulation reproduces the control-flow effect of an abend and not its transactional effect: no backout of the preceding policy insert occurs, so the harness cannot demonstrate the rollback the four `LGSQ` sites exist to trigger [base/src/lgapdb01.cbl:393,431,477,551]. Stated here because no other artifact may carry it. | `modernization/harness/stubs/cics_abend.cbl`; rule R8 of `modernization/harness/translate.py`; the abend items of `modernization/harness/copybooks/hcapture.cpy`. |
| D-31 | Put a **diagnostic-link stub in place of the linked program**: every literal diagnostic `LINK` becomes a call to one harness stub that counts the calls, rather than resolving the program the source names. | (a) Read the linked program's implementation and emulate its behaviour. (b) Drop the diagnostic links from the generated copy. | The linked program is outside the authorized source surface, so reading it is not permitted; option (b) would change the control flow of every error path. Counting the calls preserves the flow and the observable fact that the chain tried to report, without depending on an implementation this work may not inspect. | The content of a diagnostic message is not verified, only that a link occurred and how many times. The nine sites supply four different area lengths, so the stub must accept any of them. Accepted deliberately: message content is not part of the interface this work preserves. | `modernization/harness/stubs/cics_diag_link.cbl`; the nine sites at [base/src/lgapol01.cbl:149-166; base/src/lgapdb01.cbl:575-592; base/src/lgapvs01.cbl:169-185]. |
| D-32 | Leave the **diagnostic area bound without reference**: that stub binds the area as a one-byte item and never reads, moves, displays, reference-modifies or assigns to it. | Declare each of the four area shapes and read the one that matches the length. | Binding without reading is the minimum the call requires and is length-agnostic, so one module serves all nine sites. Reading the area would import the layout of an out-of-scope program into a harness module and make the stub sensitive to a shape it has no authority over. | Nothing about the diagnostic payload is captured, so a malformed message would be invisible. Consistent with D-31: the payload is out of scope, and the stub performs no output at all — no queue, no file, no terminal, no clock and no environment read. | `modernization/harness/stubs/cics_diag_link.cbl`. |
| D-33 | Pass the two **WRITE length operands as 5-digit literals**: zero-padded alphanumeric literals of that width, read by `PIC 9(5)` receivers in the capture module. | Pass them as numeric literals and let the compiler choose the operand representation. | A literal operand of a dynamic call needs a definite length on both sides, and a fixed five-digit width makes the caller's literal and the callee's receiver agree by construction for both operands — the 64-byte record length and the 21-byte key length [base/src/lgapvs01.cbl:137,139]. An unpadded numeric literal would bind different widths at the two sites. | The width is a harness convention with no counterpart in the source, so a reader could mistake `00064` for a source spelling. Mitigated by the translator asserting the exact file name, record length and key length and by all six write operands reaching the capture module unchanged. | `WRITE_LENGTH_LITERAL_DIGITS` and the write rule of `modernization/harness/translate.py`; `modernization/harness/stubs/cics_write.cbl`. |
| D-34 | Treat **chain traversal witnessed through the emulated services** as the completion criterion rather than the return code alone: a case passes only when the policy capture, the product capture and the VSAM capture are all present as its row expects. | Accept `CA-RETURN-CODE = '00'` as proof that the chain completed. | The return code is a single field in shared storage and a chain that ended early could leave it at a value that looks successful. The three captures are written by three different modules reached through two links [base/src/lgapol01.cbl:121-124; base/src/lgapdb01.cbl:243-246], so their joint presence is independent evidence that both links were taken. | Presence of a capture proves a stub ran, not that a real Db2 or VSAM write would have succeeded. The harness emulates those services and this row is the only place that limit is recorded. | `modernization/harness/stubs/sql_insert_policy.cbl`; the per-case capture assertions of `modernization/harness/run_harness.sh`; `modernization/harness/copybooks/hcapture.cpy`. |
| D-35 | Have the **chain-populated windows seeded outside the produced domain**: every COMMAREA window the chain is expected to write starts at a value that window cannot legitimately produce — the return code with a two-digit value that is not a valid code, the policy number with zeros and the timestamp with spaces. | Seed them with spaces or zeros generally, or with plausible values. | It makes chain authorship provable rather than assumed. A post-chain value inside the domain can only have been written by the chain, and a window still holding its seed is visibly untouched. It also lets the one case that abends before any return code is set assert the seed itself [base/src/lgapol01.cbl:98-105]. | The seeds are sample-construction artifacts and appear in no canonical row, so a reader inspecting the generated record could mistake one for data. Mitigated by the builder rejecting a seed inside the declared domain, a seed of the wrong length, a non-digit seed for a numeric window, a missing item, a repeated item or an item that is not chain-populated. | The `chain_populated_items` block of `modernization/extraction/copybook_field_map.yml`; `modernization/extraction/build_sample_commarea.py`. |
| D-36 | Have **evidence paths recorded relative to the repository root**, never as an absolute path. | Record absolute paths, which is what the tooling naturally produces. | Evidence has to read the same from any checkout and has to be comparable between two runs, and an absolute path embeds a location that differs per clone. Relative paths also keep the evidence free of host and user details. | A relative path is ambiguous without knowing the root it is relative to. Mitigated by stating the convention in the evidence itself, so each published file declares that its paths are repository-root-relative. | The path normalisation in `modernization/harness/run_harness.sh`; `modernization/validation/artifacts/runtime-versions.txt`; `modernization/validation/artifacts/evidence-manifest.sha256`. |
| D-37 | Express the amount threshold as a module constant in the comparison tool, with no command-line option and no environment override, and fail the run when the field map's declared threshold disagrees with it. | Expose a `--tolerance` flag so an operator could adjust it per run. | The threshold is an acceptance contract, not a runtime preference: an operator able to widen it could turn a failing comparison into a passing one without leaving a trace. Cross-checking it against the field map means the documented threshold and the applied threshold cannot drift apart silently. | Changing the threshold requires a code change and a new row in this log, which is the intent rather than a drawback. A stale declaration in the field map stops the run instead of being ignored. | `AMOUNT_TOLERANCE` in `modernization/validation/diff_harness_vs_warehouse.py`; `amount_tolerance_abs` in `modernization/extraction/copybook_field_map.yml`; D-04 above. |
| D-38 | Guard the raw load with delete-then-load on the natural key `(source_system_key, policy_number)`: a delete of that key followed by an insert locally, and by a `COPY` on Amazon Redshift. The delete predicate compares **each key field with `IS NOT DISTINCT FROM` rather than with `=`**. | (a) Truncate and load, which would erase the first sample when the second loads. (b) Append blindly, which duplicates a row on every re-run. (c) Compare the key fields with `=`, the operator a key predicate is normally written with. | The whole pipeline must be idempotent while retaining both samples in the raw relation at once, and only a key-scoped delete satisfies both. Both loaders reach the key from the same landing contract, so the two targets behave identically. On the operator, (c) is rejected on the shape of the relation rather than on an expected value: the predicate is generated once over the whole natural-key tuple, and both members are nullable columns of the all-`VARCHAR` raw relation under D-41, so it has to be null-safe by construction to be correct for every value the column type admits. Against a value the two operators agree, so a successful row is replaced exactly as `=` would replace it, and the null-safe form costs nothing where the value is present. | It relies on the natural key being genuinely unique per sample, which the distinct identity seeds of D-19 guarantee for this exercise. A future source that reused a policy number under one source-system key would overwrite rather than duplicate. The null-safe comparison also matches a stored null to a null key value, a row the landing contract cannot produce — extraction admits only return code `00` and the landing schema admits `'00'` alone under D-51 — so the operator guards the declared nullability of the column and not a case any run of this bridge can reach. | `build_delete_statement` and `NATURAL_KEY_FIELDS` in `modernization/landing/load_local.py`; the delete and `COPY` statements of `modernization/landing/load_redshift.sql`; the nullable key columns of `modernization/warehouse/ddl/02_raw_genapp_policy_issue.sql`; the uniqueness tests under `modernization/dbt/genapp_rqi/tests/`; D-41 and D-51. |
| D-39 | **SUPERSEDED BY D-80.** Write one fixed object name, `part-0000.json`, under each mandated prefix. As shipped, that name is the default part of D-80 and the object name carries a part element, so one prefix holds one object per part. | Generate a unique object name per run, so every landing is separately retained. | It kept the prefix contract simple and made the object addressable by the loaders without a discovery step. What the row did not weigh is replay: with one name per prefix the second landing of a day removed the object of the first, so the landing zone could not rebuild the raw relation and a manifest-driven `COPY` could reach one record only. D-80 keeps the addressable-without-discovery property — the loaders still rebuild the key rather than list the prefix — and adds per-record identity inside the object name. | As this row stood, landing and loading had to proceed one sample at a time and the raw relation retained both samples only because of the key-scoped load of D-38. That constraint is lifted by D-80: both objects of a two-sample run coexist and either can be reloaded on its own. The `OBJECT_NAME` constant this row cites still exists in both tools and now holds the default part's name alone. | `OBJECT_NAME` in `modernization/landing/land_to_s3.py` and in `modernization/landing/load_local.py`; `modernization/landing/partition-layout.md`; D-80 and D-81 below. |
| D-40 | Have the Redshift `COPY` read a one-entry manifest object beside the data object rather than the data key directly. | Point the `COPY` at the object key, substituting the prefix into the statement. | Amazon Redshift binds no parameter inside a `COPY` location, so a directly-addressed key would have to be assembled by string substitution into the statement itself. A manifest moves the variable part into an object the landing writer produces, and a one-entry manifest also makes a missing object fail the load loudly instead of loading nothing. | Two objects must now stay in step per landing, and a stale manifest would load the wrong data. Mitigated by writing both objects in the same landing step and by asserting the staged row count immediately after the `COPY`. | `MANIFEST_OBJECT_NAME` in `modernization/landing/land_to_s3.py`; the `COPY` and the staged-count assertion in `modernization/landing/load_redshift.sql`. |
| D-41 | Type the raw relation as all-`VARCHAR`, nullable, with widths taken from the source PICTURE of each field, and perform every conversion in dbt. | (a) Type the raw relation properly and convert at load time. (b) Use unbounded text for every column. | Raw must preserve exactly what landed, including a value that fails conversion, or a load-time cast would discard the evidence a comparison needs. Source-derived widths add a cheap structural check at the boundary without implying a business type; option (b) would give up even that. | A width is only a boundary check and not validation: a numeric field can still hold non-numeric text within its width, and the dbt casts are where that surfaces. Deliberate, so the failure is visible in transformation rather than at load. | `modernization/warehouse/ddl/02_raw_genapp_policy_issue.sql`; `modernization/landing/landing-schema.json`; `modernization/dbt/genapp_rqi/models/intermediate/int_policy_issue_decoded.sql`. |
| D-42 | Let dbt own the creation of both canonical relations, and limit the warehouse DDL to bootstrapping the two schemas and the raw relation. | Write canonical `CREATE TABLE` statements as DDL and have dbt select into them. | Two declarations of one column set drift. The enforced dbt contracts are the single declaration of the canonical shape, and dbt builds each relation's column list from that block, so a hand-written DDL copy could only ever be redundant or wrong. | The canonical shape exists only inside the dbt project, so nothing outside dbt can create those relations. Accepted: dbt is the executable schema definition for this bridge, and the contracts fail the build on a renamed, added, dropped or retyped column. | `modernization/warehouse/ddl/01_schemas.sql`; `modernization/warehouse/ddl/02_raw_genapp_policy_issue.sql`; `_canonical__models.yml` and the two mart models under `modernization/dbt/genapp_rqi/models/marts/canonical/`. |
| D-43 | Return a declared custom schema name verbatim, trimmed of surrounding whitespace, with nothing prefixed or appended, so the literal schemas `staging`, `intermediate` and `canonical` are what the models land in on both adapters. | Keep dbt's default behaviour, which prefixes the target schema to a custom schema name. | The canonical schema name is a stated requirement, and the default behaviour would produce a prefixed name that satisfies it on neither adapter. Overriding the resolution once, in one macro, keeps every model's declared schema honest without repeating the name anywhere. | Two targets writing to the same literal schema names could collide in one warehouse, which is why per-target separation is a connection concern and not a model concern. Noted as the reason a real deployment sets its own schema for the Redshift target. | `modernization/dbt/genapp_rqi/macros/generate_schema_name.sql`; the layer defaults of `modernization/dbt/genapp_rqi/dbt_project.yml`. |
| D-44 | Resolve every credential, endpoint, bucket, path and target selection from environment variables, and commit a profile **template** rather than a profile. | Commit a working profile with placeholder values, or read credentials from a committed configuration file. | A committed profile is the shortest path to a committed credential, and a template that names each variable is enough to make the contract explicit. It also keeps target selection a run-time decision, as D-05 requires. | An unset variable becomes a run-time failure rather than a build-time one. Deliberate: a missing credential must stop the run loudly rather than fall back to a default, and the failure names the variable it needed. No secret, bucket name, account identifier or role reference appears in any tracked file. | `modernization/dbt/genapp_rqi/profiles.example.yml`; the environment handling of `modernization/landing/land_to_s3.py` and `modernization/landing/load_local.py`. |
| D-45 | Pin both dbt adapters and both database drivers in the dependency manifest regardless of which branch runs. | (a) Install only the adapter the active branch needs. (b) Split the manifest into a local file and a real-target file, selected per branch. | Switching to the real target later must require configuration and credentials, not a dependency-manifest edit — the same reasoning as D-09, applied to the environment instead of the models. Both adapters installed also means the Redshift profile can be parsed and validated on the local branch, and one manifest cannot drift from a second one that no branch exercises. | A slightly larger dependency surface, accepted deliberately. It is bounded: ten direct pins, every one exact, with a clean dependency check. | `modernization/requirements.txt`; `modernization/validation/artifacts/runtime-versions.txt`. |
| D-46 | Use no dbt package and publish no `packages.yml`. Cover every assertion with built-in generic tests plus four singular tests. | Adopt a general-purpose dbt package such as a utilities library for its ready-made tests. | The built-in `not_null`, `accepted_values` and `relationships` tests cover the column-level assertions, and the four singular tests carry what no built-in expresses: uniqueness of the natural key on each relation, row-for-row parity with the intermediate model in both directions, parity of the key set between the two relations, the relation inventory of the canonical schema, the physical column shape of both relations, the product premium null pattern on the canonical relation and on the landed relation (D-101), the declared column widths read from the parsed project (D-103) and the value, sign and magnitude of the six amounts. A package would add a dependency to restate part of that. | Some assertions are hand-written rather than library-provided, so they carry their own correctness burden. Bounded to four files, each asserting against the same declarations the contracts use. | The four tests under `modernization/dbt/genapp_rqi/tests/`; the absence of `packages.yml` in `modernization/dbt/genapp_rqi/`. |
| D-47 | Ignore exactly five generated paths from a **nested `.gitignore`** inside `modernization/` — the virtual environment, the harness build tree, the dbt target and log directories, and the local DuckDB database — and keep `validation/expected/` and `validation/artifacts/` tracked. | (a) Add the repository's root ignore file instead, which D-01 forbids. (b) Add convenience patterns for editor, cache and operating-system files. (c) Ignore the two evidence directories along with the other generated output. | A nested file keeps the whole ignore contract inside the tree it governs without touching a pre-existing file, and five explicit roots are auditable in a way a pattern list is not — every authored file stays tracked by construction. Option (c) is the one that matters: those two directories are the evidence deliverable and have to be reviewable in the repository, even though their contents are generated. | An incidental artifact dropped into a tracked evidence directory would be committed, because nothing there is ignored. Accepted in exchange for evidence visibility, and bounded by the harness republishing its own evidence set on every run so a name it owns cannot linger. | `modernization/.gitignore`; the published evidence set of `modernization/harness/run_harness.sh`; `modernization/validation/artifacts/`; `modernization/validation/expected/`. |
| D-48 | Accept the measured runtime versions and report each difference from the specification's pinned versions as a named deviation, rather than failing the build. Keep a strict mode available to make any compiler difference fatal. | (a) Fail the run on any version difference. (b) Report nothing and let the difference pass unnoticed. | The pinned distribution packages are not available in the configured suites of the measured host, so option (a) would make the work unrunnable for a reason no code change can fix. Reporting each difference with its required and measured value keeps the deviation visible and reviewable, which is what the pin exists to achieve. Every Python dependency pin matches exactly, so the deviation is confined to system runtimes. | A different compiler or interpreter version could behave differently from the pinned one. Mitigated by recording the resolved compiler dialect chain with a hash per file, the numeric-store keys the harness depends on, and a capability probe per behaviour the harness relies on — rather than assuming compatibility. | `modernization/validation/artifacts/runtime-versions.txt`; `modernization/validation/artifacts/compile.log`; the strict-version switch of `modernization/harness/run_harness.sh`; `modernization/requirements.txt`. |
| D-65 | Extend the gate C exempt inventory of the read-only gate to the nine stage records `modernization/Makefile` republishes under `modernization/validation/artifacts/` — the version report of `verify-env`, the recorded selection and the two probe logs of `gate`, the compiler output of `compile`, the harness output of `execute`, and the clean, run and test output of `dbt` — taking the inventory from fourteen to twenty-three exact paths. | (a) Stop tracking those nine, leaving the recorded gate selection and the stage logs out of the reviewable evidence set. (b) Leave the inventory unchanged and accept that `make all` cannot complete a second time. | The `gate` stage of the one-phase order runs before the first read-only check and rewrites `gate-selection.json` and both probe logs, each of which carries the time and the probe result of the run that wrote it, so an unchanged inventory makes the order unrunnable rather than merely noisy: measured on this checkout, gate C reported three tracked modifications and the verdict `FAIL-TRACKED-MODIFICATION` at stage `baseline`, before any generated output. Option (a) would remove from the tracked evidence exactly the artifacts D-47 keeps tracked on purpose. The inventory stays exact-path, so no prefix, directory or pattern becomes exempt with them. | Nine more paths whose modification gate C no longer counts, so a hand edit of one of them is not caught by that gate. Bounded three ways: each of the nine is rewritten from measured output by the stage that owns it on every run and none is authored, `runtime-versions.txt` stays outside the inventory, and the five source hashes together with every other tracked path — an authored path under `modernization/` included — stay under the gate. | `GENERATED_EVIDENCE_EXEMPT` in `modernization/validation/verify_readonly.sh`; the stage records named by `modernization/Makefile`; `modernization/validation/artifacts/readonly-check.log`; D-47 above. |
| D-66 | Adopt **per-statement capture without deduplication**: every capturing stub holds its own slot for each host value its statement was handed, so one source item read by five statements is captured five times — `HC-IDENT-POLICYNUM`, `HC-LCHG-POLICYNUM`, `HC-MOT-POLICYNUM`, `HC-COM-POLICYNUM`, `HC-END-POLICYNUM` and `HC-HOU-POLICYNUM` each hold the assigned policy number — and the driver compares the copies against each other and against the identity the harness assigned. | (a) One deduplicated slot per logical value, written by the first statement that carries it and read by every later one. (b) Capture the policy number once, at the identity statement, and omit it from the product groups. | The capture is the harness's only witness of what each statement was actually handed, and a shared slot cannot distinguish a statement that passed the right value from one that passed nothing. `DB2-POLICYNUM-INT` [base/src/lgapdb01.cbl:117] is bound as an operand at seven statement sites — the identity `SET` [base/src/lgapdb01.cbl:309], the `WHERE` predicate of the timestamp read-back [base/src/lgapdb01.cbl:320] and slot 1 of every product insert [base/src/lgapdb01.cbl:357,378,418,461,524] — captured in six slots, the two endowment branches sharing one under D-17, so a copy per statement is what turns the equality of those readings into an assertion. It also keeps each stub's captured slot count equal to the operand count the statement map declares for it, 7 + 1 + 2 + 9 + 7 + 10 + 20 = 56 host-variable slots, so the group can be checked against the source rather than against a deduplication rule. | The group is larger than the set of distinct values it holds, and a reader could mistake two copies of one value for two values. Mitigated by each item's comment naming its statement, its slot and the source item it witnesses, and by the driver asserting the copies: the read-back predicate against the returned identity, and every product copy against the identity assigned for that case, so a divergence is a reported failure rather than a silent one. | The `HC-SQL-*` groups of `modernization/harness/copybooks/hcapture.cpy`; `checks.using_counts` in `modernization/harness/statement_map.yml`; the `CHECK-SQL-BOOKKEEPING`, `CHECK-IDENTITY-VALUES`, `CHECK-MOTOR-VALUES`, `CHECK-COMMERCIAL-VALUES` and `CHECK-HOUSE-VALUES` paragraphs of `modernization/harness/driver.cbl`. |
| D-67 | Hold the written record as a **flat VSAM payload capture**: one 64-byte group carrying the three key items in the declaration order of the source's key group and the remaining 43 bytes as a single alphanumeric item. Reproduce none of the four product overlays. | (a) Reproduce all four `REDEFINES` overlays and decode the one the request type selects. (b) Capture the record as one unstructured 64-byte item. | The write passes `From(WF-Policy-Info)` with `Length(64)` and `Ridfld(WF-Policy-Key)` with `KeyLength(21)` [base/src/lgapvs01.cbl:135-141], and that group is a 21-byte key of three items followed by a 43-byte data area [base/src/lgapvs01.cbl:25-30]. Because the capture receives the record image by position, its item order has to follow the storage declaration [base/src/lgapvs01.cbl:27-29] and not the order the program moves the items in [base/src/lgapvs01.cbl:99-101], which is an execution order rather than a layout. What this work asserts of the record is chain completion, the length and the composite key, so the key is captured item by item and the payload whole. Option (a) would import four product layouts [base/src/lgapvs01.cbl:31-51] that no canonical column maps to; option (b) would leave the key — the part being asserted — unaddressable. | Nothing inside the 43-byte payload is asserted, so a product overlay written into the wrong bytes would be captured without being noticed. Bounded: the payload carries no canonical value, the VSAM capture corroborates completion and the key alone, and the image is captured in full, so those bytes stand in the evidence even where no assertion reads them. | `HC-VSAM-RECORD` in `modernization/harness/copybooks/hcapture.cpy`; the record move of `modernization/harness/stubs/cics_write.cbl`; the per-case VSAM assertions of `modernization/harness/run_harness.sh`. |
| D-68 | Keep the **Ridfld operand captured apart from the record image**: the 21 bytes passed as the key operand are moved into a group of their own, and the group holding the record image receives the record operand and nothing else. | (a) Capture the record image alone and read its leading 21 bytes as the key. (b) Capture the key alone and treat the record as derivable from it. | The command supplies two operands that address overlapping storage — `WF-Policy-Key` is the first 21 bytes of `WF-Policy-Info` [base/src/lgapvs01.cbl:25-29,135-141] — but they reach the emulated service as two arguments, and only two independent readings can show that what was presented as the key is what stands at the head of the record that was written. Option (a) assumes the very overlap it is meant to witness, and option (b) discards the payload the length assertion covers. | Two readings of overlapping source storage can disagree only where the harness itself bound the wrong argument, so in a passing run the second group is redundant — which is the intended state. Its cost is 21 bytes of shared storage and one further move per write, and each group is filled from the argument it was handed rather than copied from the other. | `HC-VSAM-RIDFLD` and `HC-VSAM-RECORD` in `modernization/harness/copybooks/hcapture.cpy`; the two moves of `modernization/harness/stubs/cics_write.cbl`; D-33 above. |
| D-69 | Limit the **EIB surrogate storage scope** to the five fields the chain references — `EIBTRNID`, `EIBTRMID`, `EIBTASKN`, `EIBCALEN` and `EIBRESP2` — in one `EXTERNAL` group, with no `VALUE` clause on any item and all five seeded procedurally by the driver before the chain is called. | (a) Reproduce the whole EXEC Interface Block as the platform declares it. (b) Declare the five separately in each program instead of sharing one group. (c) Give the items `VALUE` clauses so a module compiled on its own would find them initialised. | The chain references five fields and no more [base/src/lgapol01.cbl:88-91,98,113,154-156; base/src/lgapdb01.cbl:151-154,165,210,339,580-582; base/src/lgapvs01.cbl:97,143,173-175], so option (a) would make the surrogate a claim about the platform rather than about the source. Option (b) would break the property the chain depends on: the caller sets the COMMAREA length before each link and the callee reads it [base/src/lgapol01.cbl:121-124; base/src/lgapdb01.cbl:243-246], so the three programs and the driver must address one instance. Option (c) cannot work either, because the length is not a constant of the build: the case table runs 32,500, 20 and 0 through the same copybook, and the last of those is the value the zero-length abend path requires [base/src/lgapol01.cbl:98-102]. | A module run by anything other than this driver finds the five items at the initial byte of the run unit rather than at a seeded value, so any other caller has to seed them itself. Accepted: the harness has one entry point, the driver, and it seeds all five in one paragraph before every call. | `modernization/harness/copybooks/dfheiblk.cpy`; the `SEED-EIB-FIELDS` paragraph of `modernization/harness/driver.cbl`; the chain-link rule of `modernization/harness/translate.py`, which emits a move into `EIBCALEN` ahead of each dynamic call; D-26 above. |
| D-70 | Declare the surrogate's **EIBCALEN binary representation** as `PIC S9(4) COMP-5` rather than `PIC S9(4) COMP`, and leave every receiver inside the chain exactly as the source declares it. | (a) `PIC S9(4) COMP`, matching the spelling of the chain's own length locals. (b) Widen the field to `PIC S9(9) COMP`, so no length arithmetic anywhere could truncate. | The length carried across both links is 32,500 [base/src/lgapol01.cbl:121-124; base/src/lgapdb01.cbl:243-246], which does not fit four decimal digits, and under the mandated `-fbinary-truncate` of D-23 a `PIC S9(4) COMP` surrogate keeps only the digits its PICTURE declares — the chain would then be handed a length it was never called with. `COMP-5` holds the full halfword the platform field holds, which is what a surrogate for that field must carry. What the chain does with that value afterwards is source behaviour and is preserved unchanged: it is copied into `PIC S9(4) COMP` locals [base/src/lgapol01.cbl:33; base/src/lgapdb01.cbl:33; base/src/lgapvs01.cbl:23] and is the minuend of the endowment length subtraction [base/src/lgapdb01.cbl:339-340], and those receivers do truncate. That truncation is exactly what the harness exists to characterise (D-22), and D-18 records the route it reaches. Option (b) would change the width of the interface field itself. | Two spellings of a halfword now stand side by side, a `COMP-5` surrogate feeding `COMP` locals, and a reader could take the surrogate's spelling for the source's. Mitigated by measurement rather than assertion: the version report records 32,500 unchanged in the `COMP-5` surrogate against 32,348 without the option and 2,348 with it in the `PIC S9(4) COMP` receiver of that subtraction, so which item truncates and which does not is a recorded value. | The `EIBCALEN` declaration of `modernization/harness/copybooks/dfheiblk.cpy`; the truncation probe of `modernization/validation/artifacts/runtime-versions.txt`; D-18, D-22 and D-23 above. |
| D-71 | Keep the **compiler environment pinned for the harness compile and execution**: before the first compile a run sets `COB_CONFIG_DIR` to the configuration directory the compiler reports as its own, leaves `COB_RUNTIME_CONFIG` unset so a module loads the run-time configuration of that installation, and sets `COB_CFLAGS` to the C options the compiler reports with the host toolchain's duplicated `_FORTIFY_SOURCE` definition reduced to the one that takes effect. Any of the three the caller exported is removed first and reported as a deviation naming the ignored value. | (a) Compile and run under whatever the caller's environment carries. (b) Pin the three to values this project chooses rather than to the installation's own. (c) Keep both `_FORTIFY_SOURCE` definitions and accept the host diagnostic they produce on every compile. | The dialect a compile resolves, the run-time configuration a module loads and the C options the compiler hands its own back end together decide what the generated code does with a numeric item, so leaving them to the caller would make the characterisation unreproducible and would let an exported value change a result silently. Option (b) would substitute a guess for the installation's own configuration and could name a dialect chain the compiler does not ship. On (c), the host states the definition twice, both reach one command line and the later one takes effect, so keeping that one changes no compilation and clears diagnostics that would otherwise bury a COBOL one: measured, 16 host redefinition warnings before the pin, 0 after it, and a COBOL warning still reported. | The run reports what it pinned rather than what the caller intended, so an operator who set one of the three sees it ignored. Deliberate and visible: each replaced value is reported as a deviation naming it, and the compile log records the resolved dialect chain with a SHA-256 per file together with the numeric-store keys that chain resolves, so the configuration a result was produced under is part of the evidence instead of a property of the host. | `COBC_PINNED_ENVIRONMENT` and `pin_compiler_environment` in `modernization/harness/run_harness.sh`; `modernization/validation/artifacts/compile.log`; `modernization/validation/artifacts/runtime-versions.txt`; D-23 and D-48 above. |
| D-72 | Have the **read-only gate log published with the evidence set**: the gate appends its block to a log under the ignored generated build tree, the preflight of a run empties that log, and the run publishes one copy carrying the four gate blocks of that run. | (a) Have the gate append straight to the tracked evidence copy. (b) Publish only the last gate's block. (c) Let the log accumulate across runs of the checkout. | Option (a) would make every gate a write to a tracked file, which is the very thing the gate exists to detect; writing under the ignored tree means a gate run modifies no tracked state at all. Option (c) would make the published record a history of the checkout rather than the record of one run, so a reader could not tell which blocks belong to the results beside them. Option (b) would drop what the four blocks establish jointly: the five sources were unchanged before any generated output, after the compile, after the execution and at the end of the run. Each gate is asked for the guard's reproducible block, so two runs over one tracked state publish the same bytes whatever directory the checkout was taken into. | The published file is a copy, so a reader inspecting it reads a record and not a live check — the live gate is the script, which any reader can re-run — and because the set is replaced per run, an earlier run's blocks survive only in that run's own evidence set. Accepted: the record of a run is that run's evidence. | `SOURCE_GUARD_LOG`, `reset_source_guard_log`, `run_source_guard` and `PUBLISHED_STAGE_ARTIFACTS` in `modernization/harness/run_harness.sh`; `modernization/validation/artifacts/readonly-check.log`; D-47 and D-65 above. |
| D-73 | Hold **the three signals deferred for the replacement of the published evidence set**: for that window alone, termination, interrupt and hangup are handled by a recorder that keeps the first of them and returns success; the replacement then completes, the terminating handlers are reinstalled and the recorded signal ends the run. | (a) Keep the terminating handler installed throughout, ending the run at whichever file the signal arrived on. (b) Ignore the three signals for that window, letting the run finish normally. (c) Leave the window unguarded and repair a half-replaced set afterwards. | The replacement clears this run's names, publishes the staged files under them and re-reads every published file against the manifest, so a run ended inside it would leave tracked evidence half replaced: some names of this run beside some of the previous one, and no manifest describing either. Deferring for that window is the narrowest scope that leaves the set whole, and unlike (b) it still honours the signal — the recorded one is what ends the run once the set is complete. The recorder returns success deliberately: a non-zero return would end the run at the interrupted command under the shell's error and pipeline settings, which is the state the deferral exists to prevent. Option (c) would need repair logic for a state no run should be able to reach. | An interrupt is honoured later than it was sent, bounded by one replacement. Bounded further: a signal delivered to the whole process group also reaches the command the running step started, and such a step either reports the name it could not write and ends with the evidence status or ends with the signal status, in both cases leaving the set replaced as far as that step reached and saying so. A second signal of the three is held the same way and adds nothing. | `defer_signal`, `defer_signal_traps`, `resume_deferred_signal`, `install_signal_traps` and `stage_evidence` in `modernization/harness/run_harness.sh`. |
| D-74 | Build the **house fixture derived from the motor record**: the runner writes it by overlaying the request id and the six house items onto the generated motor record, leaving every other byte as that record carries it, and no sample definition is authored for it. | (a) Author a third sample definition and have the record builder emit the house record, as it emits the motor and commercial ones. (b) Drop the two house cases and leave the house route compiled but unexercised, as the endowment route is left under D-18. | What the house cases need of a record is that the route can be reached and completed: the request id `01AHOU` selects it [base/src/lgapdb01.cbl:190-192] and the insert moves `CA-H-VALUE` and `CA-H-BEDROOMS` into binary host variables [base/src/lgapdb01.cbl:405-406], so those two windows have to hold digits, while the other four house items are alphanumeric and the route reads nothing beyond the six [base/src/lgapdb01.cbl:405-424; base/src/lgapvs01.cbl:117-122]. The overlay supplies exactly that, and the bytes it leaves — the header and common policy items ahead of the overlay, and `CA-H-FILLER` after it [base/src/lgcmarea.cpy:56-63] — already satisfy the header validation and the policy insert. Option (a) would add a sample definition, a landing path and a canonical expectation for a product this work maps no premium for; option (b) would give up a third `00` route and a second subtype-failure abend site. | The record is generated rather than authored, so outside the overlay it carries motor values under a house request id. Bounded by claiming nothing from it: **no house-specific canonical value is claimed anywhere in this work**, the house cases assert routing, the house insert capture, the return code, the abend and the VSAM write only, and the fixture is never extracted or landed. | `HOUSE_FIXTURE`, `HOUSE_FIXTURE_OVERLAY` and `DERIVED_FIXTURES` in `modernization/harness/run_harness.sh`; the `derived_samples` block of `modernization/extraction/copybook_field_map.yml`; D-18 above. |
| D-76 | Reserve the **`HC-` capture prefix** for every item of the shared capture group, rather than naming a capture item after the source item it witnesses. | (a) Spell each capture item with the source item's own name, so a reader maps witness to witnessed by name alone. (b) Use no prefix and rely on the group qualification of the enclosing `01`. | The capture group is `EXTERNAL` and is copied into the driver and all twelve stub modules, several of which also copy `LGCMAREA`, `LGPOLICY`, `DFHEIBLK` and `HSQLCA` into the same compilation unit, so a capture item spelled like the item it witnesses would be a duplicate name in a unit that declares both — and in shared `EXTERNAL` storage a name collision would make the witness and the witnessed the same bytes, which would let a capture appear correct because it is the value it was supposed to be checking. One reserved prefix removes the possibility by construction: no item declared in the five authorized sources begins `HC-`, measured as 0 occurrences across `base/src/lgapol01.cbl`, `base/src/lgapdb01.cbl`, `base/src/lgapvs01.cbl`, `base/src/lgcmarea.cpy` and `base/src/lgpolicy.cpy`. | A reader cannot map a capture item to its source item by name and has to read the item's comment. Mitigated by every item of the group naming the source item and the locator it witnesses in the comment directly above it, and by the translator failing when a stub's declared surface disagrees with the copybook. | `modernization/harness/copybooks/hcapture.cpy`; the `USING` surfaces of the twelve modules under `modernization/harness/stubs/`; D-26 above. |
| D-77 | Represent the normal CICS response as a data item of the receiver's own picture and usage — the **normal-response constant representation** `01 DFHRESP-NORMAL PIC S9(8) COMP VALUE +0` — rather than as a literal or a compile-time constant. | (a) Rewrite the source's response test to compare `WS-RESP` against the literal zero, dropping the named item entirely. (b) Declare the item without a `VALUE` clause and have the driver set it before each case, as it seeds the EIB and capture state. | The source compares `WS-RESP`, declared `PIC S9(8) COMP` [base/src/lgapvs01.cbl:18], against `DFHRESP(NORMAL)` [base/src/lgapvs01.cbl:142], which the platform's translator expands into the numeric value of that condition. An item of the same picture and usage as the receiver keeps the generated comparison the same numeric comparison the expansion produces, so the translated test is a rename and not a rewrite — option (a) would edit the comparison itself. `VALUE +0` supplies the value at compile time, which option (b) would not: an item the driver had to seed could be left unset by any caller other than this driver, and a stub could overwrite it, so the response test would depend on harness state instead of on a constant. | The value is the harness's own statement of what the normal condition is: none of the five authorized sources declares it, so it is taken from the platform's documented value for that condition rather than from a source citation. Bounded to one item, read-only to every module, and the unequal branch is not left untested — the injected case of D-20 drives the write to report a response other than normal and the chain to return `80` [base/src/lgapvs01.cbl:142-147]. | `modernization/harness/copybooks/dfhresp.cpy`; the response test of the generated `LGAPVS01` and the `DFHRESP(NORMAL)` rule of `modernization/harness/translate.py`; the `01AMOT-RC80` row of the case table in `modernization/harness/run_harness.sh`. |
| D-78 | Declare a **minimal SQLCA field set**: the surrogate carries the enclosing `01 SQLCA EXTERNAL` group and the single member `SQLCODE PIC S9(9) COMP-5`, and no other field of the platform's SQL communications area. | (a) Reproduce the full DB2 SQLCA layout — the identifier, byte count, `SQLERRM`, `SQLERRD`, the warning flags and `SQLSTATE` — as the precompiler would supply it. (b) Declare `SQLCODE` as a standalone `01` item and drop the enclosing group. | The Db2 program references exactly one member of that area and references it nine times: it is evaluated by the `EVALUATE` at [base/src/lgapdb01.cbl:290], compared against zero after each subtype insert at [base/src/lgapdb01.cbl:389,427,473,547] and moved to `EM-SQLRC` at [base/src/lgapdb01.cbl:564]. `SQLSTATE`, `SQLERRM`, `SQLERRD`, `SQLWARN`, `SQLCABC` and `SQLCAID` are referenced nowhere in the authorized sources, measured as 0 occurrences in `base/src/lgapdb01.cbl`, so option (a) would assert a platform layout this work has no authority over and add storage no statement reads. The enclosing group is kept because the generated program reaches the field through `COPY HSQLCA.` in place of `EXEC SQL INCLUDE SQLCA` [base/src/lgapdb01.cbl:124-126], and keeping the group preserves the name the include would have introduced, so option (b) would make the substitution a rename as well as a replacement. | A source statement that later referenced another member of the area would fail to compile rather than read an unset field. That is the intended failure mode, not a gap: a missing field stops the harness and names itself, where a declared-but-never-set field would return a value the emulation never established. Bounded by being checkable against the source — the declared field set is exactly the set of SQLCA members the authorized sources reference. | `modernization/harness/copybooks/hsqlca.cpy`; the `include_sqlca` entry of `modernization/harness/statement_map.yml`; the `SQLCODE` assignments of the seven modules under `modernization/harness/stubs/` that emulate an SQL statement; D-26 above. |
| D-79 | Publish each chain-traversal witness under the name of the **emulated service that records it** — `DB2_POLICY_INSERT_REACHED`, `DB2_POLICY_INSERT_EIBCALEN`, `VSAM_WRITE_REACHED` and `VSAM_WRITE_EIBCALEN` — in place of the link names `LINK_DB2_*` and `LINK_VSAM_*`, and keep the older spellings readable through the capture-key alias list of `modernization/harness/run_harness.sh`. | (a) Keep the `LINK_*` names and add a genuine link-site witness, which means copying the capture group into the generated copies of `LGAPOL01` and `LGAPDB01` and adding a link-site rule to the translator. (b) Keep the names and disclose the discrepancy in the evidence documents alone. | The two flags are set by `modernization/harness/stubs/sql_insert_policy.cbl` and `modernization/harness/stubs/cics_write.cbl` when their own service runs, so each witnesses a service and not a link: case `01AXXX-RC99` returns `99` from the routing at [base/src/lgapdb01.cbl:184-207], a value only `LGAPDB01` writes, while both flags read `N` because no service ran there — a key named for the link therefore reported `N` for a link that demonstrably happened. Option (a) adds a capture site inside the generated copies, which changes the translation rule set, the `structural_counts` of `translation-report.json` and the chain-link transformation the specification fixes, for evidence readability alone; option (b) leaves the misleading name in every capture file. Renaming the four published keys leaves the observed values, the assertion set and both link-length assertions unchanged, and the returned `CA-RETURN-CODE` remains the witness of how far a chain that reached no service travelled. | A reader or consumer keyed on the old spellings meets a renamed key. Mitigated by keeping `LINK_*_CALEN` and `CHAIN_*_CALEN` as trailing alternatives in the runner's alias list — a run of the previous driver revision still passed all 902 assertions — and by no `LINK_*` key appearing in `modernization/validation/expected/**` or in `modernization/validation/diff_harness_vs_warehouse.py`. The published evidence artifacts carry the new names from the republish that accompanied this change onward. | `EMIT-CHAIN-CAPTURES` and `CHECK-ONE-CHAIN-WITNESS` of `modernization/harness/driver.cbl`; the `HC-CHAIN` group of `modernization/harness/copybooks/hcapture.cpy`; `CAPTURE_KEY_NAMES` and `assert_case_outcome` of `modernization/harness/run_harness.sh`; QA finding Issue 1 of the F6 feature group; D-34 above. |
| D-80 | Make the part element of the landed object name variable — record `part-<NNNN>.json` and manifest `part-<NNNN>.manifest.json`, four zero-padded digits, `0000` to `9999`, selected by `--part` on both landing tools and defaulting to `0000` — so one landing prefix carries one object per part, and give `modernization/Makefile` a part per case (`01amot` = `0000`, `01acom` = `0001`) that fails rather than defaults when a case has no part of its own. | (a) Keep one fixed object name and rely on the `land`, `load`, `land`, `load` interleaving of the `all` target, which is the state D-39 recorded. (b) Add a fourth Hive-style partition segment, such as `part=0001/` or `request_id=01ACOM/`. (c) Derive a unique name per run from a timestamp or from the policy number of the record. | Under one fixed name the object identity was `(source_system_key, entity, extract_date)`, so the second sample of a day replaced the first: object storage could not be replayed to rebuild both raw rows and a manifest-driven `COPY` could only ever load one of the two. A part element restores per-record identity inside the object name and leaves the three mandated partition fields and the S3-prefix-only partitioning scope of D-10 untouched, which option (b) would change. Option (c) would make the key unpredictable to the loader, which rebuilds the key rather than discovering it. Defaulting to `0000` keeps every existing caller's key byte-identical. | A caller that reuses one part for two different records still replaces, and a caller that lands a new part without loading it leaves an object no raw row reflects. Mitigated by the per-case part mapping in the Makefile failing an unmapped case, by the replacement warning the writer still emits on a key that already carries an object, and by the loader refusing a `--key` that names a part other than the resolved one. | `OBJECT_NAME_TEMPLATE`, `resolve_part`, `build_landing_key` and `build_manifest_key` in `modernization/landing/land_to_s3.py`; the same helpers with `confirm_landing_key` and `resolve_object_key` in `modernization/landing/load_local.py`; `${LANDING_PART}` in `modernization/landing/load_redshift.sql`; `CASE_PART_*` in `modernization/Makefile`; `modernization/landing/partition-layout.md`; QA finding Issue 2 of the F8 feature group; D-38, D-39 and D-81. |
| D-82 | Give the comparison gate a **transform-freshness precondition**: before it opens the warehouse it reads dbt's own `target/run_results.json` and publishes no verdict unless every node recorded there carries a successful status, where `success` and `pass` are successful and `warn` passes but is named in the report. The path is a new `--dbt-run-results` option defaulting to `modernization/dbt/genapp_rqi/target/run_results.json` resolved from the tool's own location. | (a) Leave stage ordering in `modernization/Makefile` as the only protection, which is what it was. (b) Have the `dbt` stage write a bespoke stage-success marker for the gate to read. (c) Have the gate invoke or interrogate dbt itself. | A published verdict has to be about a warehouse state some transform actually produced, and stage ordering only protects the gate inside `make all`: invoked directly after a failed transform it certified a stale state as PASS. dbt writes the run artifact on every invocation and it is authoritative about that invocation, so reading it protects the direct call as well as the pipeline and needs no change to the Makefile or to the exempt evidence inventory of the read-only gate. Option (b) duplicates state dbt already records and can drift from it; option (c) would make the gate a dbt driver. | dbt writes the artifact only for an invocation that reaches its first node, so an invocation that fails earlier leaves the previous artifact standing: the gate therefore records the invocation id, moment, dbt version, subcommand and target it read rather than proving identity, and the `dbt clean` that opens the `dbt` stage is what removes an earlier artifact. A `make diff` on a tree whose dbt target directory was cleaned now fails where it previously passed, which is the intended direction. | `read_transform_freshness`, `transform_is_fresh`, `--dbt-run-results` and the `transform_freshness` report object of `modernization/validation/diff_harness_vs_warehouse.py`; the `dbt` and `diff` stages of `modernization/Makefile`; QA finding Issue 3 of the F12 feature group; D-83 below. |
| D-83 | Return the existing `EXIT_WAREHOUSE_REFUSED` status `3` for a freshness refusal rather than adding a status to the documented five. | (a) Add a sixth status, `6`, for the new precondition. (b) Reuse `4`, the configuration and usage status. (c) Reuse `2`, the harness-input status. | A warehouse state whose transform is not established is warehouse content the tool refuses, which is what status `3` already states, and every consumer of the published five-status map keeps working unchanged. Option (b) is wrong on its own contract: status `4` promises that nothing is written, while a refusal must still publish both reports so the refusal itself is evidence. Option (c) names an input the harness produced, which this is not. | Status `3` no longer distinguishes a refused canonical inventory from a refused transform. Mitigated by both reports naming which of the two occurred — the refusal appears in the reported-failures list and in its own precondition section, with the offending node, its status and dbt's own message. | The `FRESHNESS_REFUSED_STATUS` binding and the HOW IT FAILS block of `modernization/validation/diff_harness_vs_warehouse.py`; D-82 above. |
| D-84 | Materialize the intermediate model as a **table** rather than a view, so every cast of the bridge is evaluated once while that model builds and ahead of either mart. | (a) Keep the view, under which each mart evaluated the casts it happened to read. (b) Keep the view and duplicate the amount casts into `canonical_issued_policy` so both marts would fail together on a bad amount. | Under the view the casts were evaluated per consumer, so a landed value outside the landing contract failed only the mart that read the column: `canonical_issued_policy`, which selects no amount, rebuilt while `canonical_preissued_rating` errored, and the two relations — each defined as one row per successful issued policy and source system — could be observed disagreeing. With a table the offending value fails the intermediate model itself, dbt skips both marts, and the two relations keep the mutually consistent state of the last run that built them both. Option (b) would put amount columns into a relation whose contract does not declare them, which the canonical column set forbids. | The 17-column record is now persisted, which costs storage and one more relation to rebuild per run, and catalog evidence for `intermediate.int_policy_issue_decoded` reads BASE TABLE where it previously read VIEW. Both are stated in `modernization/validation/validation-evidence.md`. No column, type, contract, test, alias or schema changed with the materialization. | The `intermediate` key of `modernization/dbt/genapp_rqi/dbt_project.yml`; `models/intermediate/int_policy_issue_decoded.sql` and `_int__models.yml`; QA finding Issue 4 of the F15 feature group; D-85 below. |

| D-106 | Record the **read-only stage records where they actually stand**: the evidence document cites the seven `verify-readonly` stage blocks of `modernization/Makefile` to the console of the run and, for the four that follow the `execute` stage, to the git-ignored gate log, and describes the published `modernization/validation/artifacts/readonly-check.log` as the four `harness-*` blocks it carries. | (a) Have the Makefile stage recipe append its block to the tracked `modernization/validation/artifacts/readonly-check.log`. (b) Publish a companion tracked stage log beside the evidence set. (c) Leave the citation naming an artifact that does not carry the blocks. | Option (a) is the alternative D-72 already settled: a gate that writes a tracked file does the very thing the gate exists to detect, which is why the gate accepts a log only inside the ignored generated log directory. Option (b) would place beside the published set a name no run of `modernization/harness/run_harness.sh` writes, which that script reports as a deviation, would sit outside `evidence-manifest.sha256` so a `sha256sum --check` of the directory would no longer describe all of it, and would move the gate C exempt inventory from 23 to 24 exact paths that several artifacts state. Option (c) promises a reader a record they cannot find. | The seven Makefile stage verdicts are durable only while the run is on screen and, for `execute`, `load`, `dbt` and `final`, in the ignored log until the next harness preflight empties it; the tracked record of the source guard is the four harness blocks. Bounded by the gate being a live check rather than a report: anyone needing a stage-by-stage record re-runs `modernization/validation/verify_readonly.sh --stage <label>` and reads its own block. | §1, §4 and §13 of `modernization/validation/validation-evidence.md`; the `verify-readonly` target of `modernization/Makefile`; the log directory contract of `modernization/validation/verify_readonly.sh`; D-65 and D-72 above. |
| D-107 | Read **volatile run values from the artifact that carries them** rather than transcribing them: the evidence document names the artifact and the field for the time of a run and for a per-run count, and carries no absolute clock value, run identifier or per-run count of its own. | (a) Refresh every transcribed value by hand whenever the evidence set is republished. (b) Remove the values without saying where they can be read. (c) Freeze the evidence set so a republish cannot change it. | Every run republishes the set, so a transcribed clock value is stale the moment the next run finishes; option (a) makes the document's correctness depend on a manual step that has already been missed once, which is how a four-hour discrepancy reached a committed document. Option (b) would leave the reader with no route to the value. Option (c) would give up the reproducibility the harness is built for — the gate blocks are byte-identical between runs precisely because they record no clock, which is what `--reproducible` means. | A reader has to open the named artifact to see the time of a run. Bounded by naming the exact key rather than the file alone: `generated_at` in `modernization/validation/artifacts/diff-report.json` and the `generated at (UTC)` row of `diff-report.md` for the comparison, and each gate block's own `exempt generated evidence paths modified:` record for that gate's count. | §4 and §10 of `modernization/validation/validation-evidence.md`; the published artifacts named there; D-72 above. |
| D-108 | State the **printed-value claim as the values the tools withhold**: no credential, endpoint URL, host name, role identifier or password is printed by any stage, and the `land` and `load` stages do print the bucket and region they address. The printed lines themselves are left as they are. | (a) Redact the bucket name on the console the way `modernization/landing/load_local.py` already redacts the object URI, the source-system key and the policy number. (b) Keep the unqualified claim that no stage prints one of those values. | The bucket is the destination an operator must be able to confirm on the console of a stage that writes to it, it is not a credential, and no tracked file and no published artifact carries it; option (a) would also change a tool contract that the loader's own self-test asserts verbatim, to conceal a value that is not secret. Option (b) is falsifiable by running either stage once. | The terminal or CI log of a run holds the destination bucket name, so a run whose output goes to a shared log stream discloses which bucket was addressed. Nothing else of the configuration is disclosed, and the claim now says exactly that. | §5 of `modernization/README.md`; §6 of `modernization/docs/project-guide.md`; §8 of `modernization/validation/validation-evidence.md`; the header of `modernization/Makefile`; the bucket line of `modernization/landing/land_to_s3.py` and the asserted loader message in `modernization/landing/load_local.py`. |
| D-109 | **SUPERSEDED BY D-80.** Document **`CASE` as the precondition of the load stage**, not as the selector of the object it reads: `load` loads the object standing at the landing prefix, and `CASE` names the local landing record whose presence the stage requires. The part element of D-80 gives each case its own object name under the shared prefix and `load` addresses that part, so `CASE` does now select the object the stage reads and the descriptions this row settled were restored to that reading. | (a) Pass the case-derived object key, or its expected digest, into `modernization/landing/load_local.py` so the stage refuses an object landed for another case. (b) Leave the description saying the stage loads the object of the case named by `CASE`. | One object stands per source system, entity and extract date (D-39), so the prefix carries no case component to select on, and the order lands then loads one case at a time; option (a) would add a check that contradicts that contract and change a loader interface to repair a sentence. Option (b) tells a reader running the stages by hand that `CASE` protects them from loading another case's record, which it does not. | A `load` run by hand after landing a different case loads the object standing at the prefix. Bounded: the row is keyed on `(source_system_key, policy_number)` read from the record itself (D-38), so no row is mislabelled, and the `all` order always sequences `land` then `load` for one case before the next. | §4 of `modernization/README.md`; the `load` entry, the `Cases` comment and the stage 8 banner of `modernization/Makefile`; `modernization/landing/partition-layout.md`; D-38 and D-39 above. |
| D-110 | Keep the **figure-reference census hand-measured**: the per-figure list of the files citing each figure title in `modernization/docs/architecture.md` stays a measured statement, re-measured and corrected by hand when a citing file changes, with no checker script and no pipeline stage added for it. | (a) Add a census checker as a new authored file and run it as a Makefile stage. (b) Drop the census and state only that the figures are cited by name. | The authored inventory of this work is enumerated file by file in `modernization/docs/traceability-matrix.md`, which reconciles against it, so a checker would be one more authored artifact — a 63rd, beside the canonical column-width test D-105 admits as the 62nd — carrying no requirement of its own. Option (b) would remove the statement from which the by-name citation requirement is verifiable at all. | The census is only as accurate as its last measurement, and one figure's reference count was measured wrong once and corrected. Bounded by the census naming the exact titles and the exact files, so it is re-measurable in one pass over the tracked tree, and by the requirement that any edit adding or removing a figure title re-measures it. | The per-figure census of `modernization/docs/architecture.md`; the authored-file inventory of `modernization/docs/traceability-matrix.md`. |
| D-111 | Read the target name out of the gate's selection artifact through a **guarded reader**: one program that names the stage, the path, the element it refused and the remediation on a single control-free line, for a file that cannot be read, does not hold UTF-8, does not hold JSON, does not hold a JSON object, carries no `selected_target`, or carries one that is not a non-empty text value. The two refusals the reading macro already made in shell — an absent selection and an unsupported target name — keep their wording. | (a) Keep the inline one-line read, whose interpreter traceback was what a corrupt or half-written artifact produced. (b) Parse the artifact in shell with `grep` and `sed`, or add a JSON command-line tool to the dependency set for this one read. (c) Test the file with one command and then read it with a second. | The selection artifact is the file of the run most exposed to a hand edit or a partial write, and all four consumers — `land`, `load`, `dbt` and `diff` — read it through the same macro, so one guarded reader gives every one of them the diagnostic class the macro already gave for the two cases it did guard. Option (b) would add a dependency, or shell JSON parsing, for a single read; option (c) reads the file twice and can still be overtaken between the test and the read. | The refusal quotes the interpreter's own decoder message for a malformed document, so that fragment's wording follows the interpreter rather than this file. Bounded: the reader strips control characters, so the diagnostic is one line whatever the file holds, and the stage, the path and the remediation — the parts a caller acts on — are this file's own words. A reader that cannot start at all still surfaces the interpreter's error, which `verify-env` is the gate for. | `SELECTION_READER_PROGRAM` and `read_selection` in `modernization/Makefile`; the `land`, `load`, `dbt` and `diff` recipes of that file; `modernization/validation/artifacts/gate-selection.json`. |
| D-112 | Enforce **generated path containment at parse time**: every generated path variable of the Makefile is resolved before any recipe runs, and a value resolving outside `modernization/`, or an empty value, ends the invocation naming the variable, the supplied value, the resolved path and the boundary. The measured set is wider than the two directory variables a caller would reach for first — it covers the absolute forms, every generated file the stages write, the local DuckDB database and the dbt project directory. All of them stay internal, with no documented override. | (a) Leave them overridable, as the file's own header claim assumed no caller would. (b) Mark them `override` so a command-line value is ignored without a word. (c) Check containment inside each recipe that writes. | The header states that every path a recipe of the file writes stays inside the project directory, and that was false for a command-line override: the version report, the build tree, the comparison reports, the local database and the dbt project directory could each be pointed anywhere. Option (b) hides the caller's intent instead of answering it; option (c) repeats one test across a dozen recipes and fires only once work has begun. Resolving at parse time refuses before any recipe runs and names what was refused. The wider set is measured because the escape was the mechanism, not the variable: the same override reached `DIFF_REPORT`, `LOCAL_DUCKDB` and `DBT_PROJECT` exactly as it reached the two the report named. | A caller who legitimately wants output elsewhere must change this file rather than pass a variable. Deliberate: the containment is the companion of the read-only boundary, and an in-tree value is still accepted, so output can still be relocated inside the project directory. A future variable naming a generated path has to be added to the measured set or it will not be covered. | `check_generated_path` and the two `$(foreach ...)` measurements of `modernization/Makefile`; the working-directory claim in that file's header; `modernization/validation/verify_readonly.sh`, whose tracked-modification gate this containment complements; D-01 above. |
| D-113 | Validate the **requested dbt target by name**: `DBT_TARGET` carries `local_substitute`, `redshift` or an empty value, and any other value — including one naming more than one target — ends the invocation naming the value and the accepted set. The check runs at parse time, not inside the gate. | (a) Keep mapping every unrecognised value onto "the caller did not ask for the real target", which made `DBT_TARGET=redshit` a silent local run. (b) Validate at the top of the `gate` recipe alone. (c) Pass the value through to dbt and let dbt refuse it. | The value's only effect is whether a probe that did not pass ends the run, so an unrecognised value silently answers "no" on behalf of a caller who asked for the real target — the one answer they did not ask for. Parse time also catches an invocation that never reaches the gate, such as `make dbt DBT_TARGET=redshit`, which option (b) would not. Option (c) never happens: the `dbt` recipe passes the target the gate recorded, never the caller's value, so dbt sees nothing to refuse. | A caller carrying a stray `DBT_TARGET` in the environment now fails every target of this file rather than only the gate. That is the intent — the alternative is a run on a branch nobody asked for — and the refusal names the value and the accepted set, so the correction is one edit away. | `DBT_TARGET_ACCEPTED`, `DBT_TARGET_REFUSED` and `REAL_TARGET_REQUESTED` in `modernization/Makefile`; the `gate` recipe and `modernization/validation/artifacts/gate-selection.json`; §4 and §5 of `modernization/README.md`; D-05 above. |
| D-114 | Keep **published evidence retained for a bounded number of runs**: the newest five staged evidence directories under `modernization/harness/build/evidence`, the running run's own included, with `HARNESS_EVIDENCE_RETAIN` naming another count from 1 to 1000. The prune runs in the preflight of a run, while the exclusive harness lock is held and before the first source-guard gate, and only an entry standing directly in that directory, which is a directory rather than a link or a file and carries a run-identifier name, is eligible. | (a) Retain every run, as before: ten directories and 916 KB of a 2.7 MB build tree in one checkout, growing with every run. (b) Keep only the current run's set. (c) Prune after the publication step at the end of a run. (d) Accept any positive integer with no upper bound. (e) Add a cleaning target an operator has to remember to run. | A run's staged set is the reproduction material for the published evidence beside it, so more than one is worth keeping while an unbounded history is not; five keeps the recent comparisons reachable, which (b) would not. Pruning in the preflight is what makes the policy hold for a run that fails later — a failed run stages evidence too, and that is the accumulation case (c) would never reach — and holding the harness lock is what keeps one run's removal from crossing another run's staging. The bound of (d) is not cosmetic: the shell converts an over-long decimal modulo 2^64, measured as `99999999999999999999999` becoming `200376420520689663`, so an unbounded value would silently mean a different count. Option (e) leaves the policy to memory. | A superseded staged set is removed without a prompt, so an operator comparing many runs copies what they need out of the ignored build tree first. Bounded three ways: the published tracked evidence set is never touched, every entry that is not a run-identifier directory is left exactly as it stands and counted in the step line, and a removal that does not complete ends the run naming the directory rather than continuing. | `EVIDENCE_RETAIN_DEFAULT`, `EVIDENCE_RETAIN_MAX`, `EVIDENCE_RUN_ID_PATTERN`, `resolve_evidence_retention` and `prune_staged_evidence` in `modernization/harness/run_harness.sh`; §8 of `modernization/README.md`; `modernization/.gitignore`; D-72 above. |




## Section C — Deviations from a literal or obvious reading

Every row in this section departs from the most literal reading of the requirements. Each is labelled **DEVIATION** and
states what the literal reading would have been, so no difference between the specification and the build is left
unexplained.

| ID | Decision | Alternatives considered | Why this choice | Risk | Evidence and references |
|---|---|---|---|---|---|
| D-49 | **DEVIATION** from the literal reading that the enforced canonical contract pins the specification's type table on both targets. Decided: declare the specification's parameterized types in the enforced contract — `varchar(64)` for the source-system key, `char(1)` for policy type, `varchar(6)` for request id, `char(2)` for return code and `varchar(10)` for the broker reference, beside the `bigint`, `date`, `timestamp`, `decimal(8,2)` and `decimal(10,2)` columns — and accept that contract enforcement of the declared **width** reaches one adapter only. The widths are asserted instead by the project's own tests. | (a) Declare unparameterized `varchar` so both adapters compare the same thing, which would drop the specification's declared widths from the contract entirely. (b) Drop contract enforcement so no type comparison happens at all. | dbt reads both sides of the type comparison through the active adapter. Amazon Redshift carries `varchar(n)` and `char(n)`, reports the width and enforces it; DuckDB accepts each spelling, collapses it to `VARCHAR`, reports no maximum length and enforces no width, so on that adapter the contract compares the type family and not the width. Declaring the widths keeps the specification's type table visible and enforced where the platform can enforce it, while the contract still pins the column names, the column count, the type family and the nullability identically on both. Option (a) would weaken the real target to match the substitute. | The physical width is not enforced by the contract on every adapter. Mitigated by asserting the reported type, the reported width, the numeric precision and scale, the nullability and the width of every text value in the singular tests, and by stating each logical width in the column descriptions so a reader never has to infer it. | `modernization/dbt/genapp_rqi/models/marts/canonical/_canonical__models.yml`; `modernization/dbt/genapp_rqi/tests/assert_issued_policy_unique_key.sql`; `modernization/dbt/genapp_rqi/tests/assert_preissued_rating_unique_key.sql`. |
| D-50 | **DEVIATION** from pure passthrough. Decided: derive `policy_type` from the request id rather than read it from the COMMAREA, and verify the derived value against the policy SQL capture. | (a) Treat it as a COMMAREA field, which it is not — no COMMAREA item carries it. (b) Omit the column, leaving both relations without their product discriminator. | `DB2-POLICYTYPE` [base/src/lgpolicy.cpy:43] is set by the routing `EVALUATE` from `CA-REQUEST-ID` — `01AEND` to `E`, `01AHOU` to `H`, `01AMOT` to `M`, `01ACOM` to `C` [base/src/lgapdb01.cbl:184-207] — and it is the value the policy insert supplies [base/src/lgapdb01.cbl:283]. The same letter is derived independently by the VSAM program as `Move CA-Request-ID(4:1) To WF-Request-ID` [base/src/lgapvs01.cbl:99], so the derivation is the source's own, reproduced rather than invented. It is nonetheless a derivation and not a passthrough, hence this entry. | A future request id whose fourth character disagreed with the routing table would break the equivalence between the two derivations. The guard is the comparison gate, which checks the derived value against the policy capture rather than trusting the derivation. | `modernization/extraction/extract_commarea.py`; the request-id-to-policy-type table in `modernization/extraction/copybook_field_map.yml`; `modernization/validation/diff_harness_vs_warehouse.py`. |
| D-51 | **DEVIATION** from the reading that the raw and staging layers hold landed records for failure analysis. Decided: admit a record only where the returned `CA-RETURN-CODE` is `00` — extraction refuses every other recorded code with a diagnostic naming it and writes nothing — while still carrying `return_code` as a column on every layer and re-applying the `00` predicate in the issued-policy mart as a second guard. | (a) Land every record whatever its code, so the raw layer could serve failure analysis, which is the literal reading. (b) Drop the mart predicate as redundant once extraction gates the code. | `canonical.issued_policy` means one row per successfully issued policy, and only `00` denotes success [base/src/lgapol01.cbl:105; base/src/lgapdb01.cbl:293]. A failed attempt has neither a policy number nor a timestamp, because both are assigned only after the policy insert succeeds [base/src/lgapdb01.cbl:296,301,307-321], so landing one would put a keyless, undated row into a keyed relation. The failure evidence therefore lives where it is complete — the harness captures and the per-case driver log, which record the code, the condition that produced it and any abend. Keeping the predicate in the mart as well costs nothing and keeps the relation correct even if the landing contract were later relaxed. | The warehouse holds no failed attempt, so no failure analysis is possible from warehouse data alone and the `return_code` column is constant in practice. Stated here because a reader seeing that column would otherwise expect variety in it, and because a future decision to land failures would have to revisit the not-null guarantees that depend on this one. | `LANDABLE_RETURN_CODES` in `modernization/extraction/extract_commarea.py`; the closing predicate of `modernization/dbt/genapp_rqi/models/marts/canonical/canonical_issued_policy.sql`; the landed contract stated in `modernization/dbt/genapp_rqi/models/staging/genapp_class_exemplar/_genapp__sources.yml`; `_canonical__models.yml`. |
| D-52 | **DEVIATION** from the reading that a landed record carries typed values. Decided: land every one of the seventeen fields as a JSON string — twelve of them nullable — and perform all typing in dbt. | (a) Land numbers as JSON numbers and dates as typed values. (b) Land a mixture, typing only the fields that convert cleanly. | The landed record is the boundary artifact between a fixed-width COBOL record and the warehouse, and the fixed-width source has no notion of a JSON number. Option (a) would silently reinterpret a value at the boundary — a leading-zero display numeric such as `PIC 9(6)` has no faithful JSON-number form — and would put conversion failures outside the layer that is supposed to catch them. Strings keep landing lossless and leave typing in one place. | Nothing in the landed record is type-checked as a number, so a malformed value reaches the warehouse and fails there instead. Deliberate and bounded: the landing schema constrains every field by regular expression and closes the object to additional properties, so a wrong shape still fails before the object is written. | `modernization/landing/landing-schema.json`; `modernization/extraction/extract_commarea.py`; `modernization/dbt/genapp_rqi/models/intermediate/int_policy_issue_decoded.sql`. |
| D-53 | **DEVIATION** from byte-for-byte passthrough of a source field. Decided: normalise the returned 26-character `CA-LASTCHANGED` to an ISO-8601 timestamp before landing, and accept the normalised form as the value the warehouse stores. | (a) Land the 26 characters exactly as returned and normalise in dbt. (b) Land both the raw and the normalised form. | The returned value is a Db2 timestamp read back from the inserted row [base/src/lgapdb01.cbl:315-321] into a `PIC X(26)` item [base/src/lgcmarea.cpy:40], whose separator convention is not the one a warehouse timestamp parses. Normalising once, at the only place that knows the source convention, keeps the parsing rule out of the model SQL that must run unmodified on two engines. Option (b) would add a column with no canonical target, which D-11 forbids. | The landed value is not byte-identical to the source field, so the comparison must compare the normalised forms rather than the raw characters. Mitigated by constraining the landed field to an ISO-8601 pattern in the landing schema and by the comparison normalising both sides through the same documented rule. | `modernization/extraction/extract_commarea.py`; the `last_changed` pattern in `modernization/landing/landing-schema.json`; `modernization/validation/diff_harness_vs_warehouse.py`. |
| D-54 | **DEVIATION** from the reading that an absent amount is zero. Decided: represent a product-inapplicable premium as `NULL`, never as zero, on every layer that carries it. | (a) Write zero, which the fixed-width record does supply. (b) Omit the column per product, which would need one relation per product and break the two-relation limit. | The overlays redefine one storage area, so a motor record's bytes at the commercial premium positions are motor data and not a commercial premium of zero [base/src/lgcmarea.cpy:65-94]. Zero would assert a premium that was never rated. `NULL` states the only true thing: the value does not apply to this product. It also keeps a genuine zero premium distinguishable from an inapplicable one. | A consumer aggregating the amount columns must handle nulls, and a careless sum would differ from one over zeros. Mitigated by asserting the pattern rather than documenting it: one singular test requires a motor row to carry only the motor premium, a commercial row only the four commercial premiums, and any other product no product premium at all. | `modernization/dbt/genapp_rqi/tests/assert_product_premium_nullability.sql`; `modernization/dbt/genapp_rqi/models/marts/canonical/canonical_preissued_rating.sql`; the nullable amount fields of `modernization/landing/landing-schema.json`. |
| D-55 | **DEVIATION** from the source's numeric representation. Decided: type the six canonical amounts as `DECIMAL` with scale 2, although no source declaration carries an implied decimal. | (a) Type them as integers, matching the source declarations exactly. (b) Type them as floating point. | Currency belongs in a fixed-point decimal type in a warehouse, and the specification's canonical type table states scale 2 for all six. Option (b) would introduce representation error into an amount comparison for no benefit. The consequence is stated rather than hidden: the values should normally end in `.00`, because the declarations are unsigned display numerics with no implied decimal [base/src/lgcmarea.cpy:43,73,85,87,89,91] moved by plain `MOVE` into binary host variables [base/src/lgapdb01.cbl:92,100,103,105,107,109] with no arithmetic anywhere in the corpus. | A scale the source does not carry could be read as evidence that the source holds sub-unit amounts. It does not. The remaining representation risk is encoding rather than arithmetic: this harness runs on the workstation character set, so a real extract from the original platform would have to decode that installation's EBCDIC code page before any of these amounts could be trusted. | `_canonical__models.yml` amount declarations; the amount assertions of `modernization/dbt/genapp_rqi/tests/assert_product_premium_nullability.sql`; `modernization/docs/project-guide.md` (the encoding assumption). |
| D-81 | **DEVIATION** from the literal object name of the landing key. The specification writes that key once, for one landed record, ending `part-0000.json`. Decided: read `part-0000.json` as the **default part** rather than as the only name, so a single-record landing still writes exactly that key and a second record of the same source system, entity and extract date is written under its own part — `part-0001.json` for the commercial sample of a two-sample run. | (a) Hold the single literal name, which is what the specification shows, and accept that one of the two samples of a run is never retained in object storage. (b) Move the default, for example starting at `part-0001.json`, so no key ever collides with the specification's example. | The specification states the key of one landed record and states that partitioning applies to the S3 key prefix; it does not state that one source system, entity and extract date may hold one object only. Option (a) leaves the landing zone unreplayable — the object of the first sample is gone, so the raw relation cannot be rebuilt from S3 and a manifest-driven `COPY` can only ever load one of the two records — and the validation protocol of this work treats the landing zone as the replay source. Keeping `0000` as the default makes the specification's literal key the exact key a single-record landing still produces, so the deviation is additive rather than a replacement. Option (b) would move the documented literal for no gain. | A reader comparing the specification's example with a two-sample bucket listing sees two parts where the example shows one. Recorded here, in the worked examples of `modernization/landing/partition-layout.md`, in the landing section of `modernization/README.md` and in `modernization/extraction/extraction-spec.md`, each of which names the default part explicitly. | `DEFAULT_PART_NUMBER`, `OBJECT_NAME` and `--part` in `modernization/landing/land_to_s3.py`; `CASE_PART_01amot` and `CASE_PART_01acom` in `modernization/Makefile`; `modernization/landing/partition-layout.md`; QA finding Issue 2 of the F8 feature group; D-80 above. |
| D-115 | **DEVIATION** from the literal reading that this file carries the twelve targets the specification enumerates and no other. Decided: add one optional target, **`local-endpoint`**, standing outside the order `all` runs, which brings up the local-substitute S3 API surface — starting the pinned `moto` server of this virtual environment when nothing answers at the loopback endpoint `S3_ENDPOINT_URL` names, then creating the bucket `S3_BUCKET` names when that endpoint does not already hold it — and name that target in the `land` and `load` refusals. It accepts only an `http` loopback endpoint with an explicit port of 1024 or above, refuses a bucket name outside the S3 general-purpose naming rules before creating anything, and refuses to run when `DBT_TARGET` names `redshift`. | (a) The literal reading: add no target and document the four manual steps more loudly, which was the other acceptable outcome. (b) Put the same steps in a script under `modernization/` and reference it from the documentation. (c) Make it a prerequisite of `all`, so the one-phase order becomes self-sufficient on a bare environment. | `make all` is the executable order of this work, and on a bare local-substitute environment it stopped at `land` with the bucket refusal until an operator performed four steps by hand: start the endpoint, create the bucket, export the credentials and export the two `S3_*` settings. The enumerated target list is the set of **stages the order runs**; an optional helper outside that order adds no stage to it, removes none and reorders none, so the order the specification fixes is unchanged. Option (c) would put a process start inside that order and make one of its stages depend on a running server, which the order deliberately does not do — every stage of it reads no input, starts nothing and installs nothing. Option (b) would add an artifact outside the enumerated file set in order to avoid adding a target outside the enumerated target list, which trades one departure for another and hides the capability from `make`. The bucket-name refusal is there because the local endpoint is lenient where the landing writer is not: a name `moto` accepts, such as one carrying an upper-case letter, would be created here and then refused by the very next stage. | A reader could take the target for a provisioning step. It cannot provision on AWS: it accepts only a loopback `http` endpoint, every request it makes carries that endpoint, and it refuses the real branch by name, so no AWS endpoint is addressed and no AWS object is created. It does create a bucket at that loopback endpoint, which the precondition gate is forbidden to do — the gate still creates nothing and still starts nothing, and this target is neither the gate nor a stage of `all`. Its server is started detached and outlives the invocation, so an operator who wants it gone stops it by the process number the target reports. | The `local-endpoint` recipe and `LOCAL_ENDPOINT_PROGRAM` of `modernization/Makefile`; the `land` and `load` bucket and endpoint refusals of that file; §4 and §5.1 of `modernization/README.md`; D-05, D-07 and D-56 above. |


## Section D — Alternatives ruled out

These are dependency and tooling choices where an obvious candidate was rejected. Each row names the candidate, why it
was not taken and what taking the alternative instead costs.

| ID | Decision | Alternatives considered | Why this choice | Risk | Evidence and references |
|---|---|---|---|---|---|
| D-56 | Use a moto server to provide the S3 API surface on the local-substitute branch. | (a) LocalStack, the fuller local AWS emulator, which covers far more of the API surface. (b) A hand-written stub client implementing only the calls this work makes. | Docker is unavailable in the measured environment, and LocalStack's normal deployment is a container. The moto server needs no container, is already a pinned dependency, and covers the one API surface this work exercises: put and get one object under one prefix. A hand-written client would test this project's own idea of the protocol rather than the client library the real branch uses. | The emulated surface is narrower than the real service, so a real-target run remains necessary and the AWS diff stays OPEN per D-07. Bounded by using one landing writer for both endpoints, so only the endpoint differs. | `moto[s3,server]` in `modernization/requirements.txt`; the endpoint selection in `modernization/landing/land_to_s3.py`. |
| D-57 | Write the landed object through the S3 API on both branches. | Write it to the local filesystem on the local branch and to S3 only on the real branch. | A filesystem write would exercise neither the client library nor the S3 object contract — bucket, key, prefix, content and retrieval — so the local branch would prove nothing about the code path the real branch runs. One writer against two endpoints keeps that path identical. | The local branch needs a running endpoint, which is an extra moving part compared with a file write. Accepted: it is the only way the local run tests the code the real run would use. | `modernization/landing/land_to_s3.py`; `modernization/landing/load_local.py`. |
| D-58 | Compile every translated program, stub and the driver with the stable GnuCOBOL 3 series, which the specification pins at 3.1.2. | (a) The distribution's GnuCOBOL 4 candidate, which is the newer major series. (b) Build a compiler from source to obtain the pinned patch version exactly. | That candidate is an old pre-release snapshot rather than a released version, so it would introduce pre-release compiler behaviour into the one component whose whole purpose is faithful characterisation of the source. The stable series compiles every translated program, every stub and the driver, as the capability probes show. Building a compiler from source to match the pinned patch version would put an unreviewed toolchain under the one component that has to be trustworthy, for a difference the version report already discloses. | The measured compiler differs from the pinned patch version within that series, which is recorded as a reported deviation under D-48 rather than hidden. | `modernization/validation/artifacts/runtime-versions.txt`; the compiler check of `modernization/harness/run_harness.sh`. |
| D-59 | Read query results through the database driver's own rows and Python's exact decimal type. | Install a dataframe library such as pandas for result handling. | Two relations of one row each need no dataframe, and an exact decimal type is what an amount comparison against a ±0.01 threshold requires — a library defaulting to binary floating point would introduce the very representation error D-04 exists to reason about. Fewer dependencies also means fewer pins to keep compatible. | Result handling is hand-written, so column ordering, null handling and the conversion of a returned value to an exact decimal are this project's responsibility rather than a library's. Mitigated by the tool's own `--self-test`, which exercises the verdicts a passing pipeline never reaches rather than only the one it does: a mismatched non-amount value reaching the failure status and the comparison-failure exit status, an absolute amount delta of exactly 0.01 reaching the in-tolerance pass status, a delta above the threshold failing, an absent harness authority reaching the missing status, a warehouse amount carried at a scale the contract does not declare raising the scale anomaly, and the exit-status precedence the tool applies when more than one condition holds. The gate's ability to fail is therefore demonstrated by the tool itself and not inferred from a run in which nothing failed. | `modernization/validation/diff_harness_vs_warehouse.py` and its `--self-test`; `modernization/requirements.txt` (no dataframe dependency). |
| D-60 | Install no AWS Glue client, no orchestration framework and no infrastructure-as-code library. | Add them so the proposed platform could be scaffolded in the same repository. | Those services are proposed and not built per D-13, so a dependency on them would assert capability this work does not deliver. They are documented in **Figure 3 — AFTER (PROPOSED, NOT BUILT): AWS-Native Multi-RQI Warehouse** in `modernization/docs/architecture.md` and nowhere in the dependency surface. | Building the proposed platform later requires new dependencies and a new decision row rather than an already-prepared manifest. Deliberate: an unused dependency would imply delivered scope. | `modernization/requirements.txt`; **Figure 3 — AFTER (PROPOSED, NOT BUILT): AWS-Native Multi-RQI Warehouse** in `modernization/docs/architecture.md`. |
| D-75 | Hold the exact pins `dbt-core==1.12.2` and `dbt-redshift==1.11.0` and accept the transitive resolution `sqlparse 0.5.5`, whose known advisories are fixed in `sqlparse 0.6.0`, under a stated exception: **while this exception stands the dependency set is not approved for production use**. Concretely, the dbt CLI of this environment is run as a bounded batch invocation over the authored model and test set of `modernization/dbt/genapp_rqi` only — no dbt server, no RPC mode, no process that accepts SQL from a caller, and no production workload served from this environment. | (a) Move both pins together to `dbt-core==1.12.3` and `dbt-redshift==1.11.1`, whose published metadata declares `sqlparse<0.7.0,>=0.5.5` and `sqlparse<0.7.0,>=0.5.0` and so admits the fixed release; the `require-dbt-version` range `[">=1.12.0", "<1.13.0"]` of `modernization/dbt/genapp_rqi/dbt_project.yml` admits that core release. (b) Drop the Redshift adapter, leaving only `dbt-duckdb`, which declares no `sqlparse` requirement at all — which D-45 forbids, because a later real-target run must be a configuration change and not a manifest edit. | The exclusion is a property of the pinned releases rather than a choice: the installed metadata of this environment declares `sqlparse<0.6.0,>=0.5.5` for `dbt-core` 1.12.2 and `sqlparse<0.6.0,>=0.5.0` for `dbt-redshift` 1.11.0, so no resolution of these two pins can take 0.6.0. Holding them keeps every direct Python pin identical to the version the specification names and measured as matching, which is what the environment contract of this work is verified against; option (a) changes two specified exact versions and revalidates the whole environment for a package no file of this tree imports. The residual exposure is bounded to what the advisories describe — denial-of-service conditions reached while parsing SQL, and the `python` and `php` output formats, neither of which this project uses — reached only through the dbt CLI compiler and option handling and the Redshift connection layer, over a fixed authored set of models and tests, never third-party or caller-supplied SQL. | A dependency with known advisories stands in the environment, and the compensating control is operational rather than technical: it holds only while the CLI is run as described above. **Owner: the engineer who owns `modernization/requirements.txt`,** that is whoever changes a pin in this manifest next. **Review trigger: the earliest of the next dependency refresh, the first real-target Amazon Redshift run, and 90 days after the last change to `modernization/requirements.txt`** — at that point the exception is re-evaluated rather than renewed, and carrying it further requires a new row in this log rather than silence. The production restriction is not left inside a configuration comment: it is carried in the status section of `modernization/docs/project-guide.md` and in `modernization/validation/validation-evidence.md`, so no reader of the human-facing deliverables can take this environment for a production-approved one. | `modernization/requirements.txt` (the pins and the one-line pointer to this row); the installed distribution metadata under `modernization/.venv/lib/python3.12/site-packages`, `dbt_core-1.12.2.dist-info/METADATA:49` and `dbt_redshift-1.11.0.dist-info/METADATA:31`; `modernization/dbt/genapp_rqi/dbt_project.yml` (`require-dbt-version`); `modernization/docs/project-guide.md` section 1; `modernization/validation/validation-evidence.md` section 2; D-45 above. |
| D-85 | Reject both routes to atomic mart visibility that were on the table when the two canonical relations were found able to fall out of step after a failed run: **build neither mart to a staging alias with a swap on success, and keep the `dbt` stage as `dbt run` followed by `dbt test` rather than `dbt build`**. The fail-together property is obtained instead by the table materialization of D-84. | (a) Materialize both marts under a staging alias and rename or swap them into `canonical` only when both succeed, so a reader never observes one rebuilt without the other. (b) Replace `dbt run` + `dbt test` in the `dbt` stage of `modernization/Makefile` with `dbt build`, which interleaves tests with models so a failing upstream test skips the marts. | Option (a) needs adapter-specific transactional DDL — DuckDB and Amazon Redshift differ in what a rename inside a transaction guarantees — and the swap logic would have to live in the model or hook layer, ending the property of D-09 that the same unchanged model files serve both targets. Option (b) collapses the two published logs `validation/artifacts/dbt-run.log` and `dbt-test.log` into one invocation, and both names are pinned by the exempt evidence inventory of `modernization/validation/verify_readonly.sh`, so taking it would force an edit to the read-only gate itself to keep the evidence set nameable. D-84 obtains the property both options were wanted for — a value outside the landing contract now fails one model upstream of both marts, and dbt skips them together — without touching either boundary. | Cross-mart atomicity is still not guaranteed for a failure that is not a cast failure: an adapter that dropped its connection between the two mart builds would leave one rebuilt and one not. Such a state is no longer certifiable — the precondition of D-82 refuses it and `dbt test` reports it through the mart uniqueness and relationship tests — but it can exist between two runs, and closing that window needs one of the two options above and a new row here. | `modernization/dbt/genapp_rqi/dbt_project.yml`; the `dbt` stage of `modernization/Makefile`; the exempt inventory of `modernization/validation/verify_readonly.sh`; QA finding Issue 4 of the F15 feature group; D-09, D-82 and D-84. |


## Section E — Rule-conflict resolutions

The two user-specified rules — Explainability and Visual Architecture Documentation — each appear to pull against another
requirement of this work. Each apparent conflict and its resolution is recorded below.

| ID | Decision | Alternatives considered | Why this choice | Risk | Evidence and references |
|---|---|---|---|---|---|
| D-61 | Satisfy the rule-forced documentation by creating three new files under `modernization/docs/` — this log, the traceability matrix and the architecture document. | Add the required content to an existing repository document, which would keep the file count lower. | The Explainability rule and the Visual Architecture Documentation rule define required deliverables; neither requires modifying a legacy file, and modifying one would violate D-01. Creating them where every other bridge artifact lives keeps the read-only boundary intact and satisfies both rules with no conflict remaining. | Three more documents to keep consistent with the code. Mitigated by a single direction of dependency: the code defers rationale here, this log cites the artifacts, and the matrix and the architecture document each own one distinct kind of coverage. | `modernization/docs/decision-log.md`; `modernization/docs/traceability-matrix.md`; `modernization/docs/architecture.md`. |
| D-62 | Keep operational comments in code — what a construct does, what it reads, what it writes, what it asserts — and keep all rationale here, with each artifact citing the row that explains it. | (a) Move rationale into the code where the decision is implemented, which is where a maintainer looks first. (b) Strip code comments back to nothing so no rationale could leak into them. | The Explainability rule makes this log the single source of truth for "why", so option (a) would create a second, divergent source. Option (b) would make the code unreadable to satisfy a rule about rationale rather than about comments. Splitting on "what" versus "why" satisfies the rule and keeps the code maintainable. | A reader in a code file has to follow a citation to learn why. Mitigated by every deferral naming this file, and by the rows carrying stable IDs and subjects so a citation resolves to one row. | The rationale-deferral comments in `modernization/harness/translate.py`, the twelve stub modules, `modernization/extraction/copybook_field_map.yml`, `modernization/landing/load_redshift.sql`, `modernization/requirements.txt` and `modernization/validation/verify_readonly.sh`. |
| D-63 | Produce three architecture views: one before state, one built after state, and one proposed after state that is explicitly labelled as not built. | (a) One before and one after view, as the rule's minimum reads. (b) A single after view merging built and proposed elements. | The rule requires both states of a modified architecture and forbids a target state alone; it does not forbid distinguishing what was built from what is proposed. Option (b) is the failure mode the distinction exists to prevent — a proposed service rendered indistinguishably from a delivered one. Three views keep the rule satisfied and the delivery boundary unambiguous. | Three views cost more to keep current than two. Mitigated by the naming and marking convention: the built views carry a per-component delivered-or-pending marker, and the proposed view carries none and says so in its own name. | Figures 1, 2 and 3 of `modernization/docs/architecture.md`, and its figure index. |
| D-64 | Use diagrams for topology and prose or tables for runbooks, constraints, status and decisions. | Render everything the rule touches as a diagram, including the procedures and the pass/fail statements. | The rule's preference for diagrams is about architecture, where topology is the subject. A step-ordered runbook, a pass/fail contract and a decision record are not clearer as topology, and forcing them into one would lose the ordering, the thresholds and the four things every row of this log must carry. This file therefore draws nothing and cites figures by name instead. | A reader wanting the shape of the pipeline has to open the architecture document. Mitigated by referencing each figure by its exact name wherever the shape is relevant, so the target is unambiguous. | This document; `modernization/docs/architecture.md`; `modernization/validation/validation-evidence.md`; `modernization/docs/project-guide.md`. |

**No rule overrides the hard constraints of this work.** Neither Explainability nor Visual Architecture Documentation
relaxes the five-file source guard of D-01, the two-relation canonical limit of D-11, the directive against any
constructed, inferred or backfilled rating formula in D-11, or the AWS provisioning cap of D-07 and D-13. Where a rule
required a new deliverable, it was satisfied by creating a file under `modernization/`, never by touching a pre-existing
one.

## Section F — Test-infrastructure remediation after the final QA pass

The dedicated final QA pass over this project's own test infrastructure raised eight findings — four MEDIUM and four LOW,
none blocking, none an AAP-compliance, user-rule or security defect. Every one of them was a gate that did not fail for a
defect it claims to catch, an authored case set that the orchestrated entry point did not reach, a documented environment
override that broke a gate for a reason unrelated to correctness, or published evidence that named the checkout it was
produced in. The rows below record the choices made in closing them. **No row in this section changes the behaviour of
the bridge**: the canonical relations, their column sets, the landed record, the translated programs, the return-code
contract and the comparison semantics are exactly as Sections A to E describe them, and the remediation is confined to
assertions, orchestration, snapshot representation and evidence text.

| ID | Decision | Alternatives considered | Why this choice | Risk | Evidence and references |
|---|---|---|---|---|---|
| D-86 | Close every finding of that pass as an **additive change to assertions, orchestration and evidence text**, and change no model, no relation, no landed field and no translated statement. | (a) Take a finding about a silently discarded value as a reason to carry that value into the canonical layer. (b) Relax a gate whose new assertion made an existing self-test case fail, rather than restate the case. | The findings are about test efficacy, not about the data the bridge produces; a data change would have replaced a coverage gap with a specification breach — the product-specific NULL pattern of D-54 and the two-relation limit of D-11 are requirements, not defects. One existing builder self-test case did change outcome and was restated rather than accommodated (D-90). | A reader may expect a behavioural fix behind a MEDIUM finding and find only a new assertion. Mitigated by recording, per finding, the mutation that used to survive and the exit status it now produces, in `modernization/validation/validation-evidence.md`. | `modernization/validation/validation-evidence.md` §"Test-infrastructure remediation"; the eight findings themselves. |
| D-87 | Enforce the field map's **overlay geometry and measured-length reconciliation** inside the builder's map-contract check, so it runs on every field-map load and therefore inside the `translate` stage of `make all`. | (a) Assert it only in the builder's `--self-test`, which `make all` does not run. (b) Assert it only in the comparison gate, which runs last. (c) Add a separate validator invoked by its own stage. | The recorded overlay measurements were read by no tool, so a stale value — exactly the `WS-MOTOR-LEN` staleness of D-08 that the map exists to document — survived every gate. The builder is the first consumer of the map in the executable order, so failing there is both the earliest possible refusal and free; the comparison gate would have wasted translate, compile, execute, extract, land, load and dbt first. | A map whose *unselected* overlay is inconsistent now fails the selected sample too, so one bad overlay declaration fails both samples rather than one. Deliberate: a map is coherent or it is not. | `_check_overlay_geometry` and `_check_measured_lengths` in `modernization/extraction/build_sample_commarea.py`; observed `make translate` exit 2 on a stale overlay length. |
| D-88 | Derive the expected overlay length and end byte **from the overlay's own item table** — the single redefining group declaration gives the area, the items must tile it contiguously from the group offset, and the trailing protected filler absorbs the remainder — rather than comparing the recorded values with literals. | (a) Hardcode 77/177 for motor and 1102/1202 for commercial. (b) Check only that the recorded pair is internally consistent, without an independent witness. | The item declarations are the byte contract the record is actually rendered from, so they are the only independent witness to the geometry; a literal in the checker would itself go stale, which is the defect being closed. | An overlay whose COBOL layout is not one contiguous run followed by a single filler — nested redefines, or several fillers — would need the geometry model extended before it could be declared. | The item tables of `layout.motor_overlay` and `layout.commercial_overlay` in `modernization/extraction/copybook_field_map.yml`; D-08 for the measurement itself. |
| D-89 | Keep the **group-to-measurement binding in code** and require every geometry-bearing layout group to be covered by one measurement entry. | (a) Add linkage keys to the field map pointing each group at its measured members. (b) Infer the member names from the group name. | The shipped map is correct and is read by four sibling tools, so widening its contract to carry linkage was the larger change; and the recorded names are not regularly derivable — `actual_motor_overlay_length` beside `shortfall_bytes`, `actual_commercial_overlay_length` beside `commercial_shortfall_bytes`. | A third overlay must be registered in the builder or the map is refused. Fail-closed, but it makes a metadata addition a code change. | `OVERLAY_MEASUREMENTS` in `modernization/extraction/build_sample_commarea.py`; `length_constants.measured` in the field map. |
| D-90 | Reconcile **every member of the `counts` block against the entries themselves**, not only the arithmetic between members: each status, group and target-relation member equals the number of entries recording it, and no entry may carry a value no member counts. | (a) Assert only `len(fields) == 17`. (b) Assert only the arithmetic of D-02 between `counts` members. | A length-only check misses a mislabelled entry, and an arithmetic-only check is satisfied by a mutually consistent but wrong set of numbers — measured: a mutant that demotes one entry and restates the active, declaration-only, runtime and landing members together is caught only because the 16 runtime values are themselves fixed. | An intentional census change now requires the map and the builder's two census constants to move in one change, as D-02 already requires of the map, the contracts and this log. | `_check_field_census` in `modernization/extraction/build_sample_commarea.py`; the `counts` block of the field map; D-02. |
| D-91 | Restate the builder self-test case that dropped an overlay item — it asserted the *sample* refusal for an unknown key, and a map missing an overlay item is now refused as an incoherent *map* — instead of weakening the geometry check that changed its outcome. | (a) Loosen the contiguity rule so the old exit status is still reached. (b) Move the mutation to a group carrying no overlay geometry. | Reporting an incoherent map as a sample problem would misname the defect, and the unknown-key path is still covered independently by its own case. | An audit trail citing the old case name will not find it; the case now carries the name of what it proves. | `map_dropped_overlay_item` and `sample_unknown_key` in the builder's `--self-test`; observed `cases=114 passed=114`. |
| D-92 | Give `translate.py` a `--self-test` built from **in-process fixtures** — synthetic fixed-format fragments, documents and byte strings — rather than from a real translation into a temporary build tree. | (a) Translate into a `/tmp` build tree. (b) Ship fixture files under `harness/`. | The translator's own guard requires `--build-dir` to resolve to this checkout's `harness/build`, which the live harness owns under its lock, so a temporary-tree run is either refused or collides with a concurrent harness run; in-process fixtures also let each case name the exact contradicting input. | The fixtures live in the module rather than as reviewable data files, and a guard whose only trigger is a whole-program shape needs its own case rather than being covered incidentally. | `--self-test` in `modernization/harness/translate.py`; observed `cases=93 passed=93`, and `exit 5` with `self-test FAIL r7_return_becomes_goback` when rule R7 is mutated. |
| D-93 | Split rule application from census verification, and statement-map reading from statement-map validation, so both guards are callable in isolation. `translate()` is now `apply_rules()` followed by the census check, and `load_statement_map()` is read plus validate. | (a) Leave the two guards reachable only through a pinned whole-program source and the one accepted map path, so they stay untestable. (b) Reach them by patching the census from test code. | Both guards were unreachable in isolation, which is why the finding could observe them only through external mutation. The split is pure composition — proven by generated output, the source baseline and the translation report all byte-identical to the previous translator. | Two more public names that a future caller could use to skip the census or the validation; each docstring states what it omits. This split creates no artifact and changes no source-construct mapping, so the traceability matrix is unaffected. | `apply_rules`, `read_statement_map_document` and `validate_statement_map_document` in `modernization/harness/translate.py`. |
| D-94 | Report a failing self-test case as **exit status 5**, and refuse `--self-test` together with any non-default path option. | (a) Reuse the generic non-zero translation-failure status. (b) Honour supplied paths under `--self-test`. | A distinct status separates "a gate is ineffective" from "translation failed", matching the sibling tools; the matrix reads only pinned locations, so honouring a path option would silently exercise something else. | A wrapper treating any non-zero status as a translation failure loses that distinction. | The exit-status block of `modernization/harness/translate.py`; the same status in the builder, extractor, landing, loader and comparison tools. |
| D-95 | Drive the orchestrated case selection from the Makefile variable **`CASES_MODE`, defaulting to `all`**, consumed by the `execute` stage, and keep `success-only` reachable as the fast path. | (a) Add a second `execute-all` target and call it from `all`. (b) Make `execute` always run the whole table and drop the fast path. (c) Change the harness's own default. | `make all` exercised 2 of the 12 authored cases and 243 of 902 assertions, so the return-code, abend and route characterisation of D-19 and the observed return-code contract had no orchestrated entry point. The Makefile already carries per-stage overrides — `CASE`, `CASES`, `STAGE` — so one variable matches the file's idiom, leaves the stage list of `all` unchanged and needs no change in the harness the cases live in. | A caller who scripted `make execute` now runs twelve cases, about ten seconds longer; `CASES_MODE=success-only` restores the previous behaviour, and both modes publish the same two success-case evidence files. | `CASES_MODE` and the `execute` recipe of `modernization/Makefile`; observed `execute: CASES_MODE all, 12 case(s) executed with 902 assertion(s) passed`. |
| D-96 | Make the `execute` stage assert the harness's **reported selection, its per-case return-code lines and its executed count**, not merely its exit status. | (a) Trust the exit status. (b) Match only the total assertion count. | The finding is that a green top-level command concealed unexecuted cases, so the stage has to fail when the selection or the count is not the one it asked for. | The recipe now depends on three summary line formats of the harness; a reformat there fails the stage loudly rather than silently, and each check was proven by mutating a stub harness. | The `execute` recipe of `modernization/Makefile`. |
| D-97 | Make the capture snapshots **seed-independent** by symbolising the run's identity and timestamp seeds, **and** add an explicit `--refresh-snapshot` option for a legitimate re-baseline. | (a) Re-baseline the committed snapshots on every seed change. (b) Add only the refresh option. (c) Drop the seeded keys from the snapshot. (d) Rewrite the snapshot silently whenever it differs. | Both seeds are documented, supported overrides — the per-clone identity seed is required for parallel checkouts — and (a) and (b) leave a supported override failing a gate for a reason unrelated to correctness. (c) would stop asserting that the position carries the run's identity at all, and (d) is the silent re-baselining the finding warns against. Normalising keeps every position asserted and gives a re-baseline a supported path. | A symbol cannot detect a change *within* the seeded value. Mitigated because the identity and the timestamp are still compared literally against both canonical rows in the 40 column comparisons, and any capture disagreeing with the returned COMMAREA remains literal and is reported. | `snapshot_seeds`, `_snapshot_value` and `--refresh-snapshot` in `modernization/validation/diff_harness_vs_warehouse.py`; the two files under `modernization/validation/expected/`; observed `HARNESS_POLICY_NUMBER=1000101 make all` exit 0 with the snapshots untouched. |
| D-98 | Resolve the symbol **from the run's own output**, and substitute it only in the keys of a fixed position table and only where the value equals the resolved seed. | (a) Replace the seed digits wherever they appear in the document. (b) Read the seed from the environment. | Blind substitution would rewrite a customer number or any other field that happens to share those digits; reading the environment would make the snapshot agree with the request rather than with what the chain actually assigned. | A field that legitimately equals the assigned identity *and* stands in one of the listed keys would be symbolised; those keys are exactly the positions the chain fills with the identity or the timestamp. | `SNAPSHOT_IDENTITY_KEYS` and `SNAPSHOT_LASTCHANGED_KEYS` in `modernization/validation/diff_harness_vs_warehouse.py`; the symbol forms `<policy-number:10>`, `<policy-number>` and `<last-changed>`. |
| D-99 | Identify the published environment report as `modernization` **in this checkout** and name the interpreter repository-relative, instead of printing the absolute project directory. Leave `dbt-clean.log` and `execute-harness.log` as their producers write them. | (a) Print nothing identifying. (b) Print the repository root from `git rev-parse`. (c) Post-process the whole published set to strip paths. | The report was byte-identical between checkouts except for that one line, so every clone's first run dirtied it for path reasons alone; the checkout-independent voice of the read-only gate's reproducible block was already the precedent. The two logs left alone are verbatim third-party output that carries clock times, a process id, a staging directory and durations, so relativising their paths would not make either byte-stable. | An interpreter outside the repository is still printed absolute — intended, since it is not checkout identity — and a reader loses the absolute path of the project directory. | The `verify-env` recipe of `modernization/Makefile`; `modernization/validation/artifacts/verify-env.txt`; the reproducible block of `modernization/validation/verify_readonly.sh`. |
| D-100 | Refuse a **symbolic link standing at a read input** in the extraction tool, matching the translator and the sample builder, and scope the change to that tool. | (a) Follow links everywhere, relaxing the translator instead. (b) Align all five reading tools in one change. | Two of the three tools that read the source surface and the field map already refuse a link at the name itself, so refusal is the established policy and the extractor was the outlier; widening the change to the landing tools and the comparison tool would have reached files outside the finding while adding no coverage the finding asks for. | The read-input policy is still inconsistent for `landing/land_to_s3.py`, `landing/load_local.py` and the comparison tool's text reader, which follow a link at the leaf. No write in the tree traverses a link, and every read input is content-validated, so no exposure was measured; the inconsistency is recorded here rather than left implicit. | `_read_bounded_bytes` in `modernization/extraction/extract_commarea.py`; observed exit 4 for a symlinked `--commarea` and for a symlinked `--field-map`. |
| D-101 | Assert the **landed product allocation inside the existing product-premium singular test**, as a fourth family of named rules reading the dbt source. | (a) Add a separate landed-layer singular test. (b) Express it as a built-in generic test on the source. (c) Stop nulling an inapplicable premium in the intermediate model so the existing mart rules would see it. | That file already owns the product-allocation rule family and its output shape, so the rules belong beside their siblings; a built-in test cannot express a cross-column conditional and this project publishes no `packages.yml` (D-46); and (c) would change canonical data and break the NULL pattern of D-54 to gain a test. | The test now depends on the dbt source as well as on the two models, so a source rename reaches one more node. | `assert_product_premium_nullability.sql` under `modernization/dbt/genapp_rqi/tests/`; observed `FAIL 1` for a fire premium standing on a motor row in the raw relation. |
| D-102 | Read landed presence exactly as staging reads it — `nullif(trim(value), '')` — so the text `0` counts as **present**, and restrict the landed rules to landed rows carrying `return_code = '00'`, carrying the landed policy number in the detail column rather than casting it. | (a) Read presence numerically, which would treat `'0'` as absent. (b) Apply the landed rules to every landed row. (c) Cast the landed policy number to the canonical key type. | The landing schema states that an inapplicable premium is null and that no zero stands in for it, and a zero standing in an inapplicable premium was one of the variants that survived; `'00'` rows are the rows the marts materialise, so a landed row that reaches no canonical relation cannot fail the suite and the correct-positive behaviour for a `98` row is preserved; and the landed column is `VARCHAR`, so casting it would raise a conversion error on a non-digit value instead of reporting the breach — no portable `try_cast` spans both adapters. | A product contradiction on a non-`00` landed row is caught by the landing JSON Schema alone, and a landed breach is keyed by `source_system_key` plus detail text rather than by a typed policy number. Both are stated in the test's own header. | The landed leg of `assert_product_premium_nullability.sql`; the `allOf` rules of `modernization/landing/landing-schema.json`; `stg_genapp__policy_issue.sql`. |
| D-103 | Assert the canonical column **widths from the parsed dbt project** — the declared `data_type` of the enforced contract against the required type and against each mart model's own cast — in a new singular test, and keep the catalog-width legs of the two unique-key tests. | (a) Leave the width claim unasserted locally until the OPEN Redshift run closes. (b) Assert widths only through `information_schema`, which reports none for `VARCHAR` on the local adapter and so no-ops there. (c) Write adapter-specific catalog SQL. | The enforced contract compares the type family and not the width on an adapter that collapses `varchar(n)` and `char(n)` (D-49), so a width regression in a mart cast passed locally and the width claim of the specification rested on documentation alone. Both sides of the new comparison are project text, so one file runs unchanged on both adapters, which is the portability requirement of D-09. | The test reads the `cast(<column> as <type>) as <column>` shape both marts use; a cast written another way fails loudly as an unrecognised shape rather than passing unread. | `modernization/dbt/genapp_rqi/tests/assert_canonical_column_widths.sql`; observed `FAIL 1` with `declared data_type char(1); model casts to varchar(20)`. |
| D-104 | Fold type spellings that name the same declaration — `character varying` with `varchar`, `character` with `char`, `numeric` with `decimal`, `int8` with `bigint`, `timestamp without time zone` with `timestamp` — while comparing width, precision and scale **literally**. | Compare the declared type text verbatim. | Those spellings are the same declaration on Amazon Redshift, so a verbatim comparison would report a difference where none exists; the width, precision and scale are the properties actually under assertion. | A future adapter spelling not in the fold list is reported as a difference; the list is in one place in the test. | `modernization/dbt/genapp_rqi/tests/assert_canonical_column_widths.sql`. |
| D-105 | Add **one authored artifact** beyond the 61 the plan enumerates — the canonical column-width test — and record the count as 62 rather than leaving the new file out of the artifact inventory. | (a) Fold the width rules into an existing singular test to keep the enumerated count. (b) Leave the file out of the traceability inventory. | The width assertion is a distinct concern from key uniqueness and from the product NULL pattern, and folding it into either would have made a test assert two unrelated contracts; (b) would break the 100% coverage that Rule 1 requires of the matrix. The addition sits inside the directory the plan already designates for singular tests, and it creates no relation, column, formula or domain. | The authored-artifact count departs from the 61 the plan states, so any reader comparing the two must reconcile 61 with 62. Recorded here and in the count check of the traceability matrix. | `modernization/docs/traceability-matrix.md` §C.5 and §C.8; `modernization/dbt/genapp_rqi/tests/assert_canonical_column_widths.sql`. |

## Row subjects cited by sibling artifacts

Comments in the code, configuration and evidence of this tree defer their rationale to a row here by naming its subject.
Each subject below resolves to exactly one row, so a citation can be checked mechanically. The subject wording is stable
alongside the ID; neither is reused.

- `abend sites unexercised by the two passing samples` — D-21, cited by `modernization/harness/stubs/cics_abend.cbl`.
- `called abend stub records and returns` — D-30, cited by `modernization/harness/stubs/cics_abend.cbl`.
- `capture keys named for the service that witnesses them` — D-79, cited by `modernization/harness/driver.cbl` and `modernization/harness/copybooks/hcapture.cpy`.
- `chain traversal witnessed through the emulated services` — D-34, cited by `modernization/harness/stubs/sql_insert_policy.cbl`.
- `chain-populated windows seeded outside the produced domain` — D-35, cited by `modernization/extraction/copybook_field_map.yml`.
- `commercial peril operands accepted by the harness stubs` — D-12, cited by `modernization/harness/copybooks/hcapture.cpy`.
- `compiler environment pinned for the harness compile and execution` — D-71, cited by `modernization/harness/run_harness.sh`.
- `deterministic failure injection through shared harness state` — D-20, cited by the five `modernization/harness/stubs/sql_insert_*.cbl` modules.
- `diagnostic area bound without reference` — D-32, cited by `modernization/harness/stubs/cics_diag_link.cbl`.
- `diagnostic-link stub in place of the linked program` — D-31, cited by `modernization/harness/stubs/cics_diag_link.cbl`.
- `EIB surrogate storage scope` — D-69, cited by `modernization/harness/copybooks/dfheiblk.cpy`.
- `EIBCALEN binary representation` — D-70, cited by `modernization/harness/copybooks/dfheiblk.cpy`.
- `endowment route not executed` — D-18, cited by `modernization/harness/run_harness.sh`.
- `evidence paths recorded relative to the repository root` — D-36, cited by `modernization/harness/run_harness.sh`.
- `flat VSAM payload capture` — D-67, cited by `modernization/harness/copybooks/hcapture.cpy`.
- `HC- capture prefix` — D-76, cited by `modernization/harness/copybooks/hcapture.cpy`.
- `house fixture derived from the motor record` — D-74, cited by `modernization/extraction/copybook_field_map.yml`.
- `minimal SQLCA field set` — D-78, cited by `modernization/harness/copybooks/hsqlca.cpy`.
- `no called RETURN stub` — D-29, cited by `modernization/harness/translate.py` and by `modernization/validation/artifacts/translation-report.json`.
- `normal-response constant representation` — D-77, cited by `modernization/harness/copybooks/dfhresp.cpy`.
- `per-statement capture without deduplication` — D-66, cited by `modernization/harness/copybooks/hcapture.cpy`.
- `published evidence retained for a bounded number of runs` — D-114, cited by `modernization/harness/run_harness.sh`.
- `read-only gate log published with the evidence set` — D-72, cited by `modernization/harness/run_harness.sh`.
- `Ridfld operand captured apart from the record image` — D-68, cited by `modernization/harness/copybooks/hcapture.cpy`.
- `shared event-sequence ordering witness and order guard` — D-27, cited by `modernization/harness/copybooks/hcapture.cpy` and `modernization/harness/stubs/cics_abend.cbl`.
- `shared EXTERNAL harness state` — D-26, cited by `modernization/harness/stubs/cics_abend.cbl`.
- `single-superset SQL-INSERT-ENDOWMENT call` — D-17, cited by `modernization/harness/translate.py` and `modernization/harness/statement_map.yml`.
- `the fixed compile options including -fbinary-truncate` — D-23, cited by `modernization/harness/run_harness.sh`.
- `the three signals deferred for the replacement of the published evidence set` — D-73, cited by `modernization/harness/run_harness.sh`.
- `unexercised diagnostic paths` — D-21, cited by `modernization/harness/stubs/cics_diag_link.cbl`.
- `uniform stub-side capture-order guard` — D-28, cited by `modernization/harness/copybooks/hcapture.cpy` and by eight modules under `modernization/harness/stubs/`.
- `WRITE length operands as 5-digit literals` — D-33, cited by `modernization/harness/translate.py`.
- one map covering INCLUDE and DML blocks alike — D-16, cited by `modernization/harness/statement_map.yml`.
